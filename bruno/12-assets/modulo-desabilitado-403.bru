meta {
  name: Modulo ASSETS desabilitado - 403
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/api/v1/products/{{productId}}/modules/ASSETS/disable
  body: none
}

docs {
Passo 2 de 3 do teste de module-gating desta pasta. KNOWLEDGE_GRAPH e desligado antes porque depende de ASSETS.
Resultado esperado: 200, enabled false.
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("ASSETS desabilitado", function() {
    expect(res.body.enabled).to.equal(false);
  });
}
