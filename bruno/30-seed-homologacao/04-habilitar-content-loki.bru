meta {
  name: Habilitar Content Loki
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/products/{{seed30LokiProductId}}/modules/CONTENT/enable
  body: none
}

docs {
Objetivo: garantir a dependencia CONTENT no produto Loki antes de validar Knowledge Graph em bancos locais reaproveitados.
Resultado esperado: 200, idempotente quando o modulo ja estava habilitado.
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
