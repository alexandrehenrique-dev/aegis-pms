meta {
  name: Buscar nodes sem q
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/products/{{productId}}/graph/nodes
  body: none
}

docs {
Objetivo: listar todos os nodes do produto sem filtro.
Resultado esperado: 200, array.
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("body e array", function() {
    expect(res.body).to.be.an('array');
  });
}
