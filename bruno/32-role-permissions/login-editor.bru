meta {
  name: Login seed - editor
  type: http
  seq: 9
}

post {
  url: {{keycloakIssuer}}/protocol/openid-connect/token
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  grant_type: password
  client_id: {{clientId}}
  username: {{brunoRbacEditorEmail}}
  password: {{inviteActivationPassword}}
}

script:post-response {
  bru.setVar('rbacEditorToken', res.body.access_token);
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
