meta {
  name: Remover atribuicao do produto
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/products/{{s3ProductId}}/users/{{callerSubject}}
  body: none
}

docs {
Objetivo: remover a atribuicao criada na request 'Atribuir produto a usuario existente'.
Resultado esperado: 204.
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
