# SPRINT-RESULTADO.md — Artefato de continuidade entre etapas

> Ver `00_padrao_qualidade_e_arquitetura.md`, Seção 12. Este arquivo é cumulativo: cada etapa concluída adiciona **uma** entrada nova (template fixo na Seção 12.2), nunca reescreve uma entrada já existente. É o que você cola, junto do `.md` da próxima etapa, em toda conversa nova do GPT a partir da etapa 02 — substitui a memória que aquela conversa não tem.
>
> Evidências detalhadas de cada etapa ficam isoladas em `docs/sprints/backend/results/sprint-N.md`; este arquivo é o resumo consolidado que cada conversa nova recebe. As entradas das etapas 01, 02, 03, 05, 06 e 06.1 foram retroativamente reconstruídas em 2026-06-25 a partir dos respectivos arquivos `results/` — caso algum detalhe não tenha sido registrado lá no momento da execução original, o arquivo `results/sprint-N.md` correspondente prevalece.

## Etapa 01 — Estrutura do repositório e variáveis de ambiente (concluída em 2026-06-21)

**Classes criadas/alteradas:** nenhuma classe Java — etapa de fundação. Arquivos: `docker-compose.yml`, `.env.example`, `.gitignore`, `backend/pom.xml`, `backend/Dockerfile`, estrutura de diretórios (`backend/`, `frontend/`, `infra/docker/`, `infra/keycloak/`, `scripts/`, `docs/`).

**Endpoints confirmados:** nenhum endpoint implementado nesta etapa.

**Decisões de implementação registradas pelo GPT:** frontend oficial React + Vite, porta 5173 (ADR-0011); backend Java 25 / Spring Boot 4.1.x / Spring Security 7 / Spring Data JPA / Flyway / Spring Modulith; PostgreSQL como único banco oficial (nunca H2, nunca MongoDB); arquitetura monólito modular (nunca microsserviços); Keycloak como único provedor de identidade (nunca autenticação própria); prefixo global de API obrigatório `/api/v1`.

**Retrofits pendentes para etapas futuras:** Docker Compose completo, PostgreSQL do Aegis e do Keycloak, Keycloak configurado, projeto Spring Boot funcional, Flyway/Modulith configurados, realm/client/integração OAuth2 Resource Server, primeiro contrato REST sob `/api/v1` — todos resolvidos nas etapas 02-05.

**Cobertura de testes:** não aplicável — nenhuma classe de produção criada nesta etapa. Evidências completas: `results/sprint-1.md`.

## Etapa 02 — PostgreSQL dedicado do Aegis e do Keycloak (concluída em 2026-06-21)

**Classes criadas/alteradas:** nenhuma classe Java — etapa de infraestrutura. Containers `aegis-postgres` e `keycloak-postgres-aegis` (PostgreSQL 16), cada um com volume Docker nomeado e persistente, adicionados ao `docker-compose.yml`.

**Endpoints confirmados:** nenhum endpoint de domínio nesta etapa.

**Decisões de implementação registradas pelo GPT:** dois bancos PostgreSQL totalmente separados (nunca compartilhar schema entre Aegis e Keycloak); nenhum dos dois pode perder dados em `docker compose down` sem `-v`; H2 nunca é uma opção, nem em dev.

**Retrofits pendentes para etapas futuras:** subir o Keycloak conectado ao `keycloak-postgres` (etapa 03).

**Cobertura de testes:** não aplicável — infraestrutura pura, validada via healthcheck dos containers (`Up (healthy)`) e `docker compose down && docker compose up -d` sem `-v`. Evidência consolidada junto da etapa 03, já que não há `results/sprint-2.md` dedicado exclusivamente a esta etapa — ver `results/sprint-2.md` (validação dos containers operacionais) e `results/sprint-4.md` (healthcheck confirmado novamente).

## Etapa 03 — Keycloak persistente, realm/client/roles e export (concluída em 2026-06-21)

**Classes criadas/alteradas:** nenhuma classe Java. Artefato `infra/keycloak/realm/aegis-realm.json` (export do realm). Container `aegis-keycloak` adicionado ao `docker-compose.yml`, conectado ao `keycloak-postgres` da etapa 02.

**Endpoints confirmados:** `GET http://localhost:8282/realms/aegis/.well-known/openid-configuration` (OIDC discovery), validado com `HTTP 200 OK`. Nenhum endpoint REST do backend Aegis criado nesta etapa.

**Decisões de implementação registradas pelo GPT:** realm `aegis`, client `aegis-web` (OpenID Connect, client authentication OFF, standard flow ON, PKCE S256); 5 realm roles globais canônicas (`AEGIS_SUPER_ADMIN`, `AEGIS_TENANT_ADMIN`, `AEGIS_PRODUCT_MANAGER`, `AEGIS_EDITOR`, `AEGIS_VIEWER`); usuário de validação criado com `AEGIS_SUPER_ADMIN`; serviço Docker mantido como `keycloak-postgres` (hostname interno usado em `KC_DB_URL_HOST`).

**Retrofits pendentes para etapas futuras:** `directAccessGrantsEnabled` foi ativado apenas para validação manual via `grant_type=password` — não representa o fluxo oficial da SPA (resolvido pelo proxy de autenticação da etapa 06).

**Cobertura de testes:** não aplicável — infraestrutura pura. Validado via `SELECT count(*) FROM realm` (resultado: 2), persistência confirmada após restart e `docker compose down/up` sem `-v`, e export real do realm. Evidências completas: `results/sprint-2.md` (fechamento) e `results/sprint-3.md` (execução parcial anterior, mesma etapa).

## Etapa 04 — Backend Spring Boot base e conexão com PostgreSQL/Flyway (concluída em 2026-06-21)

**Classes criadas/alteradas:** `br.com.byop.aegis.AegisApplication` e `br.com.byop.aegis.shared.storage.LocalStorageBootstrap`; adicionados também `application.yml`, perfis `local`/`prod` e a migration consolidada `V1__init.sql`.

**Endpoints confirmados:** `GET /actuator/health`, respondendo HTTP 200 com `status: UP`. Nenhum endpoint de domínio foi criado nesta etapa.

**Decisões de implementação registradas pelo GPT:** o schema inicial foi consolidado em uma única migration V1; a tabela `event_publication` segue o formato exigido pelo Spring Modulith 2.0.1; o bootstrap de storage é síncrono no startup, usa Java NIO e interrompe a aplicação quando a raiz não pode ser criada; `ddl-auto` permanece em `validate`.

**Retrofits pendentes para etapas futuras:** criar as subpastas por produto/categoria quando os domínios `product` e `asset` forem implementados; adicionar configuração explícita de `SecurityFilterChain` na etapa 05; evoluir as tabelas mínimas na etapa 06 sem recriar o schema do zero.

**Cobertura de testes:** `mvn clean verify` terminou com `BUILD SUCCESS` e executou o goal `jacoco:check`; como ainda não havia execução de testes gerando `jacoco.exec`, o JaCoCo informou explicitamente que o check foi ignorado por ausência do arquivo de execução. Evidências completas: `results/sprint-4.md`.

## Etapa 05 — Security Resource Server e `/api/v1/me` (concluída em 2026-06-23)

**Classes criadas/alteradas:** `AuthenticatedUser`, `AuthenticatedUserProvider`, `JwtRoleConverter`, `SecurityConfig` (segurança); `MeController`, `MeResponse`, `MeResponseMapper` (API); testes `AuthenticatedUserProviderTest`, `JwtRoleConverterTest`, `MeResponseMapperTest`, `MeControllerTest`, `SecurityConfigTest`; testes complementares de cobertura `AegisApplicationTest`, `LocalStorageBootstrapTest`.

**Endpoints confirmados:** `GET /api/v1/me` — protegido por JWT, retorna `{ subject, email, username, name, role }` com `role` no formato canônico minúsculo (ex.: `super_admin`). `401` sem token e com JWT inválido, `200` com JWT válido.

**Decisões de implementação registradas pelo GPT:** conversão de roles `AEGIS_<NOME>` (Keycloak) → `ROLE_<NOME>` (Spring Security) → `<nome>` minúsculo (contrato `/me`) centralizada em `JwtRoleConverter` (ADR-0014); `directAccessGrantsEnabled=true` no client `aegis-web` necessário para validação manual via `grant_type=password` (uso temporário, substituído pelo proxy da etapa 06); `@EnableWebSecurity` explícito necessário para os testes `@WebMvcTest` encontrarem o bean `HttpSecurity`.

**Retrofits pendentes para etapas futuras:** o `grant_type=password` direto contra o Keycloak usado para validação manual nesta etapa não deve ser exposto ao frontend — resolvido pelo auth proxy (BFF) da etapa 06.

**Cobertura de testes:** `mvn clean verify` com Sonar e JaCoCo aprovados (100%). Evidências completas: `results/sprint-5.md`.

## Etapa 06 — Auth Proxy, SMTP e fluxo de convite (concluída em 2026-06-24)

**Classes criadas/alteradas:** `KeycloakTokenClient`, `KeycloakTokenResponse`, `KeycloakAdminClient` (clients); `AuthLoginRequest`, `AuthRefreshRequest`, `AuthLogoutRequest`, `AuthForgotPasswordRequest`, `AuthTokenResponse`, `AuthTokenResponseMapper`, `AuthMessageResponse` (DTOs); `AuthService` (orquestração); `AuthController`, `AuthExceptionHandler`, `AuthErrorResponse` (REST); `InvalidCredentialsException`, `AccountDisabledException`, `RefreshTokenExpiredException`, `KeycloakAuthenticationException` (exceções); `KeycloakProperties`, `RestClientConfig` (config). Endpoints administrativos de usuários (`GET /api/v1/users`, `GET /api/v1/users/{id}`) também entregues nesta etapa, antecipando parte da etapa 14/15. Templates `infra/keycloak/themes/aegis/email/{html,text}/executeActions.ftl` e `password-reset.ftl`, `theme.properties`, serviço `mailhog` no `docker-compose.yml`.

**Endpoints confirmados:** `POST /api/v1/auth/login`, `POST /api/v1/auth/refresh`, `POST /api/v1/auth/logout` (204), `POST /api/v1/auth/forgot-password` (200 genérico, sem enumeração de e-mail) — todos públicos, liberados em `/api/v1/auth/**` no `SecurityFilterChain`. `GET /api/v1/users` e `GET /api/v1/users/{id}` (protegidos, via Keycloak Admin API) entregues como antecipação.

**Decisões de implementação registradas pelo GPT:** `KeycloakProperties` usa defaults locais para `KEYCLOAK_ISSUER_URI`/`KEYCLOAK_INTERNAL_BASE_URL`/`KEYCLOAK_REALM`/`KEYCLOAK_WEB_CLIENT_ID` (evita falha de placeholder sem valor em dev); SMTP/sender configurado no realm Keycloak e exportado em `aegis-realm.json` (sem senha, conforme padrão) — obrigatório para `execute-actions-email` funcionar; `@PathVariable("id")` declarado explicitamente em todo path variable (evita dependência de metadata `-parameters` do compilador); CSRF desabilitado mantido para API REST stateless baseada em Bearer Token.

**Retrofits pendentes para etapas futuras:** templates `productAssignment.ftl`/`productAccessRevoked.ftl` (necessários pelas etapas 10 e 15) ficaram fora do escopo desta etapa — resolvidos na etapa 06.1. Avaliar padronização de encoding Maven (`UTF-8` explícito no `pom.xml`) para eliminar warnings recorrentes — ainda pendente.

**Cobertura de testes:** `mvn clean verify` com `Tests run: 70, Failures: 0, Errors: 0, Skipped: 0` e `All coverage checks have been met` (JaCoCo 100%). Evidências completas: `results/sprint-6.md`.

## Etapa 06.1 — Hotfix: templates de e-mail de produto (concluída em 2026-06-25)

**Classes criadas/alteradas:** nenhuma classe Java — etapa exclusiva de templates. `infra/keycloak/themes/aegis/email/html/productAssignment.ftl` e `infra/keycloak/themes/aegis/email/html/productAccessRevoked.ftl`.

**Endpoints confirmados:** nenhum — templates consumidos futuramente pelas etapas 10/15 via `JavaMailSender`/`FreeMarkerTemplateUtils`, não via `executeActionsEmail` do Keycloak.

**Decisões de implementação registradas pelo GPT:** templates mantidos em `infra/keycloak/themes/aegis/email/html/` (mesmo diretório dos templates da etapa 06), não em `backend/src/main/resources/templates/email/` — centraliza todo template de e-mail do tema Aegis num único lugar, já que a etapa 10 vai apenas renderizá-los, não depende do Keycloak para isso.

**Retrofits pendentes para etapas futuras:** a etapa 10 (`POST /products/{productId}/users` com `userId`) e a etapa 15 (remoção de `ProductAssignment`) precisam implementar o envio efetivo via `JavaMailSender` apontando para estes dois arquivos `.ftl`; validação funcional via MailHog só é possível quando a etapa 10 existir.

**Cobertura de testes:** não aplicável — sem código Java, sem testes, sem migration, conforme padrão de entrega da própria etapa. Evidências completas: `results/sprint-6.1.md`.

## Etapa 07 — Modelo Core: Tenant, Membership, Product e Catálogo de Módulos (concluída em 2026-06-25)

**Classes criadas/alteradas:** entidades `Tenant`, `TenantMembership`, `Product`, `ProductModule` e `ProductAssignment`; repositories `TenantRepository`, `TenantMembershipRepository`, `ProductRepository`, `ProductModuleRepository` e `ProductAssignmentRepository`; enums de status, papéis, tipos de produto, módulos e storage; DTOs/records `TenantSummary`, `ProductSummary`, `ProductDetail` e `ProductModuleSummary`; mappers MapStruct `TenantMapper`, `ProductMapper` e `ProductModuleMapper`; services `TenantService`, `ProductService` e `ProductModuleService`; `ProductAccessResolver`; controllers `TenantController` e `ProductController`; contracts `CreateTenantRequest` e `CreateProductRequest`; `CoreExceptionHandler`/`CoreErrorResponse`; `RequireModule`, `ModuleAccessAspect`, `ModuleDisabledException` e `ModuleProductIdMissingException`. Ajustados também `KeycloakProperties`/`KeycloakAdminClient`/`application.yml`/`.env.example` para externalizar o path da Admin API.

**Endpoints confirmados:** `GET/POST /api/v1/tenants`; `GET/POST /api/v1/products`; `GET /api/v1/products/{productId}`; `POST /api/v1/products/{productId}/modules/{moduleKey}/enable`; `POST /api/v1/products/{productId}/modules/{moduleKey}/disable`.

**Decisões de implementação registradas pelo GPT:** modelo core implementado em `br.com.byop.aegis.core`; `ProductAssignment` automático para criador do produto com role `product_manager` e status `atribuido`; listagem de produtos filtrada por papel no service; `ProductAccessResolver` centralizado para etapas futuras; dependência de módulos mantida em mapa (`KNOWLEDGE_GRAPH -> CONTENT`); `@RequireModule` exige método anotado com `@PathVariable("productId") UUID productId`; módulo desabilitado bloqueia todos os papéis, inclusive `SUPER_ADMIN`; `assetStorageStrategy` default permanece no service; path `/admin/realms/` externalizado para `keycloak.admin-realms-path`.

**Retrofits pendentes para etapas futuras:** aplicar `ProductAccessResolver` e `@RequireModule` nos domínios de conteúdo, páginas, assets, forms, analytics e knowledge graph; implementar provisionamento real do storage local/S3 conforme etapa de assets caso ainda não esteja completo; evoluir regras próprias do módulo `ECOMMERCE` apenas em sprint futura dedicada; manter collection Postman cumulativa atualizada.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 179 testes, 0 falhas, 0 erros, 0 ignorados e JaCoCo aprovado (`All coverage checks have been met`). Collection Postman da Sprint 07 validada manualmente. Alertas SonarQube for IDE removidos; nenhum pacote/import `br.com.aegis.pms`. Base package oficial mantido em `br.com.byop.aegis`. Evidências completas: `results/sprint-7.md`.

## Etapa 08 — Knowledge Graph MVP (concluída em 2026-06-25)

**Classes criadas/alteradas:** módulo `br.com.byop.aegis.knowledgegraph` organizado por responsabilidade: `domain` (`GraphNode`, `GraphEdge`, `GraphNodeType`, `GraphEdgeType`), `repository` (`GraphNodeRepository`, `GraphEdgeRepository`), `contract` (`CreateGraphNodeRequest`, `CreateGraphEdgeRequest`), `dto` (`GraphNodeSummary`, `GraphNodeDetail`, `GraphEdgeSummary`, `GraphEdgeDetail`, `GraphNeighborSummary`, `GraphRelatedSummary`), `mapper` (`GraphNodeMapper`, `GraphEdgeMapper`), `service` (`GraphConsistencyPolicy`, `KnowledgeGraphService`), `exception` (exceções específicas) e `controller` (`KnowledgeGraphController`, `KnowledgeGraphExceptionHandler`). Migration `V3__knowledge_graph_mvp.sql`. `product` e `tenant` foram reorganizados como módulos de primeiro nível, por pacotes de responsabilidade; `product.api` passou a expor `ModuleKey`, `ProductAccessPort`, `ProductReference`, `ProductReferenceService` e `RequireModule` via `NamedInterface`. Handlers de exceção específicos foram descentralizados para seus módulos.

**Endpoints confirmados:** `POST /api/v1/products/{productId}/graph/nodes`; `GET /api/v1/products/{productId}/graph/nodes?q={label}`; `GET /api/v1/products/{productId}/graph/nodes/{nodeId}`; `POST /api/v1/products/{productId}/graph/edges`; `GET /api/v1/products/{productId}/graph/nodes/{nodeId}/neighbors`; `GET /api/v1/products/{productId}/graph/nodes/{nodeId}/related`.

**Decisões de implementação registradas pelo GPT:** Knowledge Graph não depende diretamente da entidade JPA `Product`, repository ou service interno; o módulo consome somente a API pública `product.api`. `KnowledgeGraphController` usa `ProductAccessPort` para isolamento por produto e `@RequireModule(ModuleKey.KNOWLEDGE_GRAPH)` para módulo desabilitado retornar 403 `MODULE_DISABLED`. Duplicidade de node/edge é bloqueada, não tratada como upsert. Classes novas são posicionadas em pacotes por responsabilidade, nunca diretamente no pacote raiz do módulo. Cada módulo trata suas próprias exceções; `core` mantém apenas componentes globais.

**Retrofits pendentes para etapas futuras:** integração com Content/refs inline, OpenAPI e eventuais extras do Knowledge Graph (layout, órfãos, preview leve) permanecem fora deste recorte. A collection Postman cumulativa já inclui os cenários manuais da Sprint 08.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 257 testes, 0 falhas, 0 erros, 0 ignorados e JaCoCo aprovado (`All coverage checks have been met`). Auditoria Spring Modulith e homologação manual via Postman aprovadas. Evidências detalhadas da sprint: `results/sprint-8.md`.

## Etapa 09 — Health, info e readiness (concluída em 2026-06-26)

**Classes criadas/alteradas:** `SystemStatusController`, `SystemStatusResponse`, `SystemStatusService`, `SystemStatusControllerTest`, `SystemStatusServiceTest`, `ModulithArchitectureTest`; `SecurityConfigTest` foi atualizado para cobrir a exposição pública de `/actuator/health` e `/actuator/info` no recorte de segurança.

**Endpoints confirmados:** `GET /actuator/health`; `GET /actuator/info`; `GET /api/v1/system/status`.

**Decisões de implementação registradas pelo GPT:** status de banco implementado via `JdbcTemplate` com `select 1`; status do Knowledge Graph obtido por presença do bean `knowledgeGraphService`, sem importar service/repository interno do módulo; `BuildProperties` usado quando disponível e fallback `0.0.1-SNAPSHOT` quando ausente; falhas de banco são serializadas em `databaseMessage` com mensagem legível, sem stack trace bruto.

**Retrofits pendentes para etapas futuras:** nenhum retrofit obrigatório identificado. SonarQube for IDE não foi executado via CLI nesta etapa; sem alertas bloqueantes registrados pelo humano responsável na validação de integração.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 268 testes, 0 falhas, 0 erros, 0 ignorados e JaCoCo aprovado (`All coverage checks have been met`). Auditoria Spring Modulith aprovada via `ModulithArchitectureTest`. Validação de integração/manual confirmada pelo humano responsável antes do commit. Evidências detalhadas da sprint: `results/sprint-9.md`.

## Etapa 10 — Tenants CRUD completo, escopo por papel e ProductAssignment (concluída em 2026-06-26)

**Classes criadas/alteradas:** contratos `UpdateTenantRequest`, `DeleteTenantRequest`, `AssignProductUserRequest` e `ProductAssignmentSummary`; `CreateTenantRequest` passou a receber `plan` e `initialAdminEmail`; `Tenant` ganhou `plan`; `ProductAssignment` foi ajustado para o contrato com `tenantId`, `productId`, `userSubject`, `role`, `status`, timestamps e constraint única `product_id + user_subject`; `ProductAssignmentStatus` usa enum Java canônico `ASSIGNED/INVITED`, mapeado para `"atribuido"`/`"convidado"` no contrato REST; `ProductAssignmentMapper`, `TenantMapper`, `TenantService`, `ProductAssignmentService`, `TenantController` e `ProductAssignmentController`; portas de e-mail/convite/notificação de ProductAssignment, com implementação real por Keycloak Admin API e JavaMailSender/FreeMarker para e-mails; API pública de identidade em `identity.api`; handlers e exceções específicas de tenant/produto; migration `V4__product_assignment_contract.sql`; tema de e-mail Keycloak Aegis e mensagens localizadas; testes de repository, mapper, service, controller, portas e renderização de e-mail.

**Endpoints confirmados:** `GET/POST/PUT/DELETE /api/v1/tenants` (PUT e DELETE por `{tenantId}`; GET com escopo decidido pelo service); `GET /api/v1/products/{productId}/users`; `POST /api/v1/products/{productId}/users`; `DELETE /api/v1/products/{productId}/users/{userId}`.

**Decisões de implementação registradas pelo GPT:** `ProductAssignment` permanece tabela própria e não extensão de `TenantMembership`; REST nunca expõe enum Java cru, usando `"atribuido"`/`"convidado"` no mapper; `inviteEmail` usa fluxo real via Keycloak Admin API, cria usuário se necessário, configura `requiredActions: ["UPDATE_PASSWORD"]`, chama `execute-actions-email` e grava `userSubject` com o ID real do Keycloak; usuário existente recebe e-mail informativo via `JavaMailSender` + `productAssignment.ftl`; remoção recebe e-mail de revogação via `productAccessRevoked.ftl`; MailHog é o aceite em dev, não Gmail real; `emailTheme` do realm ficou `aegis`; `loginTheme` permanece `null` porque não existe tema `login/`; `StubProductAssignmentNotificationPort` permanece como TODO explícito para Sprint 25, sem criar NotificationService paralelo; os stubs de invite/e-mail ficam como fallback/teste, não como beans principais.

**Retrofits pendentes para etapas futuras:** substituir o fluxo `executeActionsEmail` por tokens Aegis quando a etapa 29 for executada; integrar notificações internas quando a Sprint 25/NotificationService existir; a etapa 28 deverá alterar o `DELETE /tenants/{tenantId}` para backup/export assíncrono antes da exclusão. SonarQube for IDE não foi executado via CLI; sem alertas bloqueantes conhecidos além dos TODOs intencionais e classificados.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 340 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado. Validação manual via curl/MailHog confirmou: `GET tenants` SUPER_ADMIN 200, `PUT tenant` 200, `DELETE tenant` com confirmação errada 400, `GET product users` 200, `POST userId` 201, `POST inviteEmail` 201, `POST userId + inviteEmail` 400, `POST` sem ambos 400 e `DELETE assignment` 204. Postman cumulativo atualizado em `postman/aegis-postman-collection.json` com a pasta `10 - Tenant e Product Assignments`. Evidências completas: `results/sprint-10.md`.

## Etapa 11 — Domínio `content` (concluída em 2026-06-27)

**Classes criadas/alteradas:** domínio `content` completo — `Content` (com record interno `Content.Edit` para evitar excesso de parâmetros), `ContentStatus`, `DifficultyLevel`, `ContentVersion`; `ContentRepository`, `ContentVersionRepository`; `UpdateContentRequest`, `ContentTransitionRequest`, `PublishContentRequest`; `ContentSummary`, `WorkflowItemSummary`, `ContentVersionSummary`; `ContentMapper`, `ContentVersionMapper`; `ContentWorkflowPolicy`, `MarkdownSanitizer`, `ContentService`; `ContentController`; exceções e `ContentExceptionHandler`. Nova API pública `knowledgegraph.api.KnowledgeGraphPort` + `KnowledgeGraphPortAdapter` (módulo `knowledgegraph` não tinha `NamedInterface` antes desta sprint). Migration `V5__content_editorial_workflow.sql`. Dependência nova: `org.jsoup:jsoup:1.18.1`. Stubs de e-mail/convite/notificação de `ProductAssignment` (Sprint 10) tiveram o marcador `TODO` convertido em Javadoc, sem mudança de comportamento.

**Endpoints confirmados:** `GET /api/v1/products/{productId}/content`; `GET .../content/{contentId}`; `PUT .../content/{contentId}`; `POST .../content/{contentId}/transition`; `GET .../content/{contentId}/versions`; `POST .../content/{contentId}/publish`; `GET .../content/edit-events`; `GET .../content/workflow-items` — exatamente os 8 endpoints do artefato da sprint, sem `POST`/`DELETE` de criação na entrega inicial (fora do escopo literal). **Adendo pós-entrega (mesma sessão):** `POST /api/v1/products/{productId}/content` foi adicionado a pedido explícito do responsável pelo repositório, para viabilizar o fluxo completo de teste manual via Postman (criar → editar → transicionar → publicar) sem seed via SQL — ver detalhamento em `results/sprint-11.md`. **Workflow editorial** (máquina de estados `ContentWorkflowPolicy`): `Draft → In Review`, `In Review → Draft`, `In Review → Published`, `Published → Archived`, `Archived → Draft`; qualquer outra transição retorna 400 `INVALID_CONTENT_TRANSITION`. **Versionamento:** toda transição/publicação grava uma `ContentVersion` nova com snapshot do conteúdo. **Publicação:** restrita a `SUPER_ADMIN`/`TENANT_ADMIN`/`PRODUCT_MANAGER` (403 `CONTENT_PUBLISH_FORBIDDEN` para `EDITOR`/`VIEWER`), via `transition` e via `publish`. **Module-gating:** `ContentController` anotado `@RequireModule(ModuleKey.CONTENT)` — módulo desabilitado retorna 403 `MODULE_DISABLED`, mesmo para `SUPER_ADMIN`. **Isolamento por produto:** acesso resolvido via `ProductAccessPort.assertAccessible` antes do `ContentService` — recurso fora do escopo do usuário retorna 404, nunca 403.

**Decisões de implementação registradas pelo GPT:** sanitização de markdown via Jsoup (`Safelist` customizado); autor resolvido via `identity.api.IdentityUserDirectory` já existente (Sprint 10); allowlist de `span class` usa os 5 valores reais já implementados no frontend (`text-aegis-red/blue/green/amber/violet` — a Sprint 18 do frontend fala em "6 cores" mas a 6ª opção é "remover cor", não uma classe nova); `KnowledgeGraphPort` criado nesta sprint para resolver violação real do Spring Modulith (`content` não pode depender de tipos internos não expostos de `knowledgegraph`) — encapsula idempotência de node/edge dentro do próprio módulo `knowledgegraph`; `doTransition` privado não-transacional extraído para evitar self-invocation entre `transition`/`publish` (ambos `@Transactional` públicos). Dois bugs de plataforma reais encontrados e corrigidos durante validação manual com a aplicação real: (1) `ObjectMapper`/exceções precisavam ser `tools.jackson.*` (Jackson 3, Spring Boot 4.1), não `com.fasterxml.jackson.*` (Jackson 2 legado, presente só transitivamente); (2) MapStruct auto-selecionava `toContractPublication` como conversor implícito para todos os campos `String`, corrigido com `@Named`.

**Retrofits pendentes para etapas futuras:** sem endpoint de criação/exclusão de `Content` (fora do escopo desta sprint) — ambientes novos precisam de seed manual até a Sprint 21 (seed inicial); `KnowledgeGraphPort.ensureContentNode` está fixo em `GraphNodeType.CONTENT`, generalizar se outro domínio (ex.: `pages`, Sprint 22) precisar reaproveitar a porta com outro tipo de node.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 453 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado (incluindo a nova fronteira pública `knowledgegraph.api`). 46 apontamentos do SonarQube for IDE tratados ao longo da sessão (detalhamento completo em `results/sprint-11.md`) — exceto um finding repetido em `ContentRepository.java` que não foi possível reproduzir (`grep` confirmou ausência do marcador `TODO` no arquivo real, em 4 verificações). Validação manual completa via `curl` contra aplicação real (`mvn spring-boot:run`), incluindo criação de conteúdo, sanitização de `<script>`, transição inválida (400), `EDITOR` bloqueado em publicação (403, via `transition` e via `publish`), módulo desabilitado (403 `MODULE_DISABLED`), isolamento cross-produto (404) e integração real com Knowledge Graph (`kg-ref` válido cria node+edge; `kg-ref` inválido rejeita com 400 antes de persistir). Postman cumulativo atualizado com a pasta `11 - Content Editorial Workflow` (14 requests, incluindo "Criar conteudo"); dois arquivos de collection legados e obsoletos (`aegis-pms-sprint-05-identity-access.postman_collection.json`, `aegis-pms-sprint-07-core-postman-collection.json`) removidos a pedido do humano responsável, já totalmente cobertos pela collection cumulativa. Evidências completas: `results/sprint-11.md`.

## Etapa 12 — Domínio `asset` (concluída em 2026-06-27)

**Classes criadas/alteradas:** domínio `asset` completo — `Asset` (com records internos `Asset.Creation` e `Asset.Metadata`), `AssetCategory`, `AssetStatus`, `AssetTag`, `AssetTagAssignment`, `AssetUsage`; `AssetRepository`, `AssetTagRepository`, `AssetTagAssignmentRepository`, `AssetUsageRepository` (Javadoc completo em todo método); `AssetMapper` (MapStruct); abstração `StorageProvider` com `LocalStorageProvider` e `S3StorageProvider` (AWS SDK v2.29.52, `software.amazon.awssdk:s3`, sem dependência separada de `s3-presigner`), `AssetFilenameSanitizer`, `AssetStorageProvisioningService`; `AssetService`, `AssetUploadValidator`; `AssetController` (11 endpoints — os 10 do artefato da sprint mais `GET /api/v1/assets/{assetId}/file` para download direto); exceções e `AssetExceptionHandler`. Nova API pública `product.api.AssetStorageStrategy` (movida de `product.domain`) e `product.api.ProductCreatedEvent`, publicado via `ApplicationEventPublisher`/`@TransactionalEventListener` (não `@ApplicationModuleListener`, que exigiria tabela `event_publication` fora do escopo). Migration `V6__asset_library.sql`. Limites de mime/tamanho por categoria configuráveis em `aegis.assets.limits` (`application.yml`), multipart até 260MB (vídeo até 250MB).

**Endpoints confirmados:** `GET/POST /api/v1/products/{productId}/assets`; `GET /api/v1/products/{productId}/assets/{assetId}`; `PUT .../assets/{assetId}/metadata`; `DELETE .../assets/{assetId}`; `GET .../assets/{assetId}/usage`; `GET/POST /api/v1/products/{productId}/asset-tags`; `DELETE .../asset-tags/{tag}`; `GET /api/v1/assets/{assetId}/resolve`; `GET /api/v1/assets/{assetId}/file`. **Module-gating:** `@RequireModule(ModuleKey.ASSETS)` em todos os endpoints sob `/products/{productId}/...` — módulo desabilitado retorna 403 `MODULE_DISABLED`. **Validação de upload:** mime type fora do catálogo da categoria → 400 `INVALID_ASSET_MIME_TYPE`; acima do limite de tamanho da categoria → 413 `ASSET_SIZE_LIMIT_EXCEEDED`; nome de arquivo ausente/em branco → 400 `INVALID_ASSET_FILENAME`. **Tags:** corpo de `POST .../asset-tags` é um JSON string literal (não objeto) — decodificado explicitamente via Jackson; nome inválido (não decodificável) → 400 `INVALID_ASSET_TAG_NAME`; duplicado → 409 `ASSET_TAG_ALREADY_EXISTS`. **Exclusão:** asset com `AssetUsage` ativo sem `force` → 409 `ASSET_IN_USE`.

**Decisões de implementação registradas pelo GPT:** pastas de storage local provisionadas automaticamente (Java NIO, sem ação manual do usuário no SO) via `AssetStorageProvisioningService`, acionado pelo `ProductCreatedEvent`; `AssetFilenameSanitizer` reescrito sem regex (loop manual de caracteres) para evitar achados de segurança do Sonar (S5850/S5852); `S3StorageProvider` usa o estilo Consumer Builder do AWS SDK v2 (`s3Client.putObject(builder -> ..., RequestBody...)`) em vez do overload por objeto `PutObjectRequest`, corrigindo S6244.

**Bugs reais de plataforma encontrados e corrigidos durante validação manual com a aplicação real:** (1) `AwsBasicCredentials.create(accessKeyId, secretAccessKey)` lança `NullPointerException` quando as credenciais S3 estão em branco (ambiente dev sem S3 configurado) — esse `NullPointerException` não tratado era traduzido pelo filtro de exceções do Spring Security em um 403 `insufficient_scope` confuso; corrigido com fallback para `AnonymousCredentialsProvider` em `S3ClientConfiguration` quando as credenciais estão em branco, e com um handler dedicado `SdkException` → 502 `ASSET_STORAGE_UNAVAILABLE` em `AssetExceptionHandler`, para que qualquer erro real de comunicação com o S3 (bucket não configurado, indisponibilidade) surja de forma clara em vez de vazar como 403. (2) `MockMultipartFile` normaliza internamente um `originalFilename` nulo para string vazia — um teste que pretendia cobrir o ramo `originalFilename == null` na verdade só cobria o ramo de string vazia; corrigido usando `mock(MultipartFile.class)` real com `getOriginalFilename()` retornando `null` diretamente.

**Retrofits pendentes para etapas futuras:** nenhum identificado nesta sprint.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 614 testes, 0 falhas, 0 erros, JaCoCo aprovado (100% linhas e branches, nível `BUNDLE`) e Spring Modulith aprovado. Três rodadas de limpeza do SonarQube for IDE realizadas na mesma sessão (13 apontamentos iniciais → 4 → 3 → 0), sem alteração de regra de negócio, contrato, endpoint, migration ou arquitetura de storage — destaque para a reescrita do `S3StorageProvider` para o estilo Consumer Builder (S6244), validada sem regressão de comportamento. Validação manual completa contra a aplicação real (`mvn spring-boot:run`), incluindo upload local e S3, rejeição de mime inválido e de tamanho acima do limite, isolamento por produto, module-gating e os dois bugs reais descritos acima. Validação automatizada da collection de testes manuais (na época, Postman; ver Sprint Técnica de migração para Bruno, abaixo) via Newman, 23/23 requests da pasta de Assets. **Observação adicional registrada no fechamento da sprint:** durante a validação manual do upload multipart desta sprint, o aplicativo desktop do Postman apresentou falha real ao enviar arquivos via `multipart/form-data` no macOS — motivo concreto que acelerou a decisão de migrar a suíte de validação manual para Bruno (ver Sprint Técnica abaixo), que passa a ser a suíte oficialmente validada a partir desta sprint. Evidências completas e detalhamento por área: `results/sprint-12.md`.

## Sprint Técnica — Migração da collection de testes manuais: Postman → Bruno (concluída em 2026-06-27)

**Arquivos criados/alterados:** `bruno/bruno.json`, `bruno/collection.bru` (variáveis compartilhadas e header `Authorization: Bearer {{token}}` herdado por toda a collection), `bruno/environments/{local,dev,homolog,prod}.bru` (`local` com `baseUrl` preenchido; os demais propositalmente vazios, sem credenciais reais), 85 requests `.bru` distribuídos em 10 pastas numeradas (`00-auth`, `01-tenants`, `02-products`, `03-product-modules`, `04-manual`, `05-knowledge-graph`, `06-health`, `10-product-assignments`, `11-content`, `12-assets`) e `bruno/fixtures/` (arquivos binários reais para upload multipart: PNG, PDF, executável com mime inválido, PNG acima do limite de 5MB). A pasta `postman/` e o arquivo `aegis-postman-collection.json` foram removidos do repositório — substituídos integralmente pela collection Bruno, sem perda de cenário (mesma cobertura funcional, request a request). `docs/api-testing/README.md` criado; `docs/sprints/backend/00_padrao_qualidade_e_arquitetura.md` (Seção 11) reescrito para descrever o fluxo Bruno em vez do fluxo Postman.

**Escopo da migração:** apenas a suíte de testes manuais mudou de ferramenta — nenhum contrato REST, endpoint, payload, regra de negócio, autenticação ou documentação funcional foi alterado. Toda variável de runtime usa exclusivamente a API `bru.getVar`/`bru.setVar` (nenhuma referência a `pm.*`/`postman.*` no repositório). Login captura `token`/`refreshToken` automaticamente (`script:post-response`); todo request autenticado consome `Authorization: Bearer {{token}}` herdado da collection.

**Decisões de implementação registradas pelo GPT — três comportamentos do Bruno CLI descobertos só por experimentação real (documentação oficial incompleta/contraditória nesses pontos):**
1. O bloco `auth:bearer { token: {{token}} }` em modo herdado (`inherit`) não repropaga variáveis definidas em runtime entre requests na mesma execução do CLI — substituído por um `headers { Authorization: Bearer {{token}} }` simples em `collection.bru`, que reinterpola `{{token}}` corretamente a cada request.
2. `@file(caminho)` em `body:multipart-form` resolve o caminho relativo à **raiz da collection** (onde está `bruno.json`), não ao diretório do arquivo `.bru` da request.
3. Quando o `Content-Type` é `application/json` (seja em modo `body:json` ou `body:text`), o runtime do Bruno normaliza/coloca aspas no conteúdo antes de enviar, se o texto não for um JSON válido — mascarando cenários de teste que precisam enviar texto bruto, sem aspas, propositalmente inválido (ex.: validar que o backend rejeita um corpo malformado). Resolvido enviando esse cenário específico com `body:text` e `Content-Type: text/plain` (default do modo `text`), já que o decode do backend não depende do `Content-Type` recebido.

**Bug de desenho de teste encontrado e corrigido durante a reconstrução (não é bug de produção):** ao criar um produto, o criador recebe automaticamente uma `ProductAssignment` `PRODUCT_MANAGER` (única por `product_id`+`user_subject`, `uq_product_assignment_user_subject`). Um request que tentava atribuir esse mesmo usuário novamente ao mesmo produto (para validar o caminho "atribuir usuário já existente") violava essa constraint e, por uma peculiaridade da cadeia de exceções do Spring Security, retornava 403 vazio em vez de 500 — corrigido removendo a atribuição automática antes do teste e restaurando-a ao final da pasta `10-product-assignments`, preservando a cobertura sem quebrar o acesso de SUPER_ADMIN aos produtos usados pelas pastas seguintes (`ProductAccessResolver` só permite acesso de SUPER_ADMIN a um produto com `ProductAssignment` ativa — ver Seção 10.5 de `00_padrao_qualidade_e_arquitetura.md`).

**Acréscimos necessários para execução sequencial via CLI (documentados request a request, sem perda de cenário original):** módulos habilitados em uma pasta e ainda necessários em pastas posteriores (`KNOWLEDGE_GRAPH`, `CONTENT`, `ASSETS`) são explicitamente desabilitados/reabilitados nos pontos de transição entre pastas — no Postman isso era resolvido manualmente, rodando pastas isoladamente; na collection Bruno, a execução completa (`npx @usebruno/cli run --env local`, sem caminho específico) processa todas as pastas em sequência única para que variáveis de runtime (tokens, IDs capturados) persistam entre elas.

**Cobertura de testes:** `npx @usebruno/cli run --env local`, a partir de `bruno/`, com **85 requests executados, 85 aprovados, 160/160 testes aprovados** — confirmado em duas execuções completas consecutivas (idempotente: tenant/produto/usuário novos a cada execução via seeds com timestamp, sem dependência de estado deixado por execuções anteriores). `mvn clean verify` no backend, no mesmo estado do repositório, mantém `BUILD SUCCESS`, 614 testes, JaCoCo e Spring Modulith aprovados — a migração não tocou nenhum arquivo de produção ou teste automatizado Java.

## Etapa 13 — Domínio `form`/`submission` (concluída em 2026-06-27)

**Classes criadas/alteradas:** domínio `form` completo — `FormDefinition`, `FormStatus`, `FormFieldType`; contracts `CreateFormDefinitionRequest`, `UpdateFormDefinitionRequest`, `UpdateFormDeliveryRequest`; DTOs `FormSummary` e `FormDetail`; mapper MapStruct `FormDefinitionMapper`; `FormPublicationPolicy`, `FormDeliveryPolicy` e `FormService`; `FormController`; exceções e `FormExceptionHandler`. Domínio `submission` — `Submission`, `SubmissionStatus`; DTOs `SubmissionSummary` e `SubmissionDetail`; mapper `SubmissionMapper`; `SubmissionService`; `SubmissionController`; exceções e `SubmissionExceptionHandler`. APIs públicas entre módulos: `asset.api.AssetReferenceService`/`AssetReference` e `form.api.FormReferenceService`/`FormReference`/`FormFieldCatalog`. Migration `V7__forms_and_submissions.sql`. Bruno atualizado com a pasta `13-forms`.

**Endpoints confirmados:** `GET /api/v1/products/{productId}/forms`; `POST /api/v1/products/{productId}/forms`; `GET /api/v1/products/{productId}/forms/{formId}`; `PUT /api/v1/products/{productId}/forms/{formId}`; `GET /api/v1/products/{productId}/forms/field-types`; `GET /api/v1/products/{productId}/forms/{formId}/submissions`; `GET /api/v1/products/{productId}/forms/submissions`; `GET /api/v1/products/{productId}/forms/{formId}/submissions/{submissionId}`; `POST /api/v1/products/{productId}/forms/{formId}/publish`; `PUT /api/v1/products/{productId}/forms/{formId}/delivery`. `@RequireModule(ModuleKey.FORMS)` aplicado nos controllers; produto com módulo desabilitado retorna 403 `MODULE_DISABLED`; isolamento por produto segue 404 via `ProductAccessPort`.

**Decisões de implementação registradas pelo GPT:** `POST /api/v1/products/{productId}/forms` foi adicionado ao contrato da sprint por necessidade real de produto — a lista inicial tinha apenas `GET`/`PUT`, mas sem `POST` a UI não conseguiria criar um formulário novo sem seed manual. `fieldsJson`, `deliveryChannelsJson` e `answersJson` foram persistidos como JSONB PostgreSQL. Upload em submission persiste `assetId` em `answersJson` e valida MIME/produto via `asset.api`, sem dependência direta de entidades/repositories internos de `asset`. `submission` consome `form.api`; `form` não consome `submission`, evitando ciclo no Spring Modulith. Por isso `responses`, `conversion` e `lastActivity` retornam valores neutros nesta etapa. Delivery valida/persiste `email`, `whatsapp`, `telegram` e `webhook`, mas não envia mensagens nem chama webhooks reais.

**Retrofits pendentes para etapas futuras:** criar read model/evento para popular contagem de responses, conversão e última atividade dos forms sem ciclo `form -> submission`; adicionar fluxo Bruno feliz de criação/detalhe de submission quando existir endpoint público de submissão; avaliar registro de `AssetUsage` quando forms passarem a gerenciar ciclo de vida de assets referenciados em answers.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 720 testes, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado. Cobertura por camada confirmada com `@DataJpaTest` para repositories, testes de MapStruct, services/policies com mocks e `@WebMvcTest` para controllers; cenários obrigatórios cobertos: publicação sem campo obrigatório, label duplicado, upload sem `acceptedFileTypes`, MIME fora da lista, webhook inválido, telegram incompleto, cross-product 404, módulo `FORMS` desabilitado 403, submissão em formulário publicado e detalhe de submission com `answersJson` completo. Apontamentos SonarQube for IDE da Sprint 13 removidos sem alterar regra de negócio, contratos, migrations, endpoints Bruno ou arquitetura: lambdas de `assertThatThrownBy` refatoradas para uma única chamada ao método sob teste e `instanceof` ajustado para unnamed pattern quando aplicável. Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: **102 requests executados, 102 aprovados, 194/194 testes aprovados**. Evidências completas: `results/sprint-13.md`.

## Etapa 14 — Domínio `analytics` (concluída em 2026-06-27)

**Classes criadas/alteradas:** novo módulo `br.com.byop.aegis.analytics` com `AnalyticsController`, `AnalyticsService`, `AnalyticsConfig` e DTOs `AnalyticsKpiResponse`, `HealthSignalResponse`, `ChannelRowResponse`, `TrendCardResponse` e `AnalyticsReportResponse`. APIs públicas de leitura analítica adicionadas nos módulos já existentes: `content.api.ContentAnalyticsService`/`ContentAnalyticsResponse`, `asset.api.AssetAnalyticsService`/`AssetAnalyticsResponse` e `submission.api.SubmissionAnalyticsService`/`SubmissionAnalyticsResponse`, todas expostas via `NamedInterface` quando necessário. Testes criados para controller, service, config e APIs públicas de content/asset/submission. Nenhuma migration, entidade ou repository novo foi criado para analytics.

**Endpoints confirmados:** `GET /api/v1/products/{productId}/analytics/kpis`; `GET /api/v1/products/{productId}/analytics/health`; `GET /api/v1/products/{productId}/analytics/channels`; `GET /api/v1/products/{productId}/analytics/trends`; `GET /api/v1/products/{productId}/analytics/reports`. Todos usam `@RequireModule(ModuleKey.ANALYTICS)` e `ProductAccessPort.assertAccessible`, mantendo módulo desabilitado como 403 `MODULE_DISABLED` e produto fora do escopo como 404.

**Decisões de implementação registradas pelo GPT:** analytics é uma camada de agregação read-only sobre APIs públicas de módulos vizinhos, sem acessar entidades/repositories internos de outros módulos. KPIs, health, trends e reports são calculados a partir de content, assets e submissions/forms; channels permanece explicitamente simulado com tráfego zerado, porque ainda não há domínio real de tracking/aquisição. O resumo consolidado para dashboard/settings ficou fora desta sprint e deve ser tratado na etapa posterior dedicada a dashboard/settings. Os contratos de `trends` e `reports` foram formalizados em `docs/trace/00_endpoints_esperados.md` a partir dos componentes frontend existentes.

**Documentação e Bruno:** Bruno atualizado com a pasta `14-analytics`, cobrindo habilitação do módulo, endpoints felizes, bloqueio por módulo desabilitado e reabilitação ao final para não quebrar pastas futuras. `docs/api-testing/README.md` atualizado para registrar `14-analytics` na árvore da collection e a dependência de `productId` herdado da execução sequencial. `docs/trace/00_endpoints_esperados.md` atualizado para formalizar os contratos de `TrendCard` e `AnalyticsReport`. `docs/sprints/backend/21_seed_inicial_e_grafo.md` preservado no fechamento com ajustes de seed já presentes no worktree: tenant principal `CLIENTES BETA` e módulo `PAGES` no produto `loki`.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 746 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado. Testes direcionados `mvn -Dtest='AnalyticsServiceTest,AnalyticsControllerTest' test` e `mvn -Dtest=AnalyticsConfigTest test` também passaram com `BUILD SUCCESS`. Apontamentos SonarQube for IDE conhecidos da Sprint 14 removidos sem `@SuppressWarnings`, falso positivo, alteração de contrato, payload, endpoints, migrations, Bruno funcional ou regra de negócio: ternários aninhados extraídos, literais duplicados movidos para constantes semânticas, assertion AssertJ consolidada e teste de clock ajustado para validar `ZoneOffset.UTC` sem relógio do sistema. Evidências completas: `results/sprint-14.md`.

## Etapa 15 — Domínio `users` (concluída em 2026-06-27)

**Classes criadas/alteradas:** domínio tenant-scoped de usuários implementado em `br.com.byop.aegis.product.user`: `InviteTenantUserRequest`, `UpdateTenantUserRequest`, `TenantUserSummary`, `TenantUserMapper`, `TenantUserService`, `TenantUserController`, `TenantUserExceptionHandler`, `TenantUserAlreadyExistsException`, `TenantUserNotFoundException`, `InvalidTenantUserOperationException` e `LastTenantAdminException`. APIs públicas criadas/alteradas para preservar fronteiras do Spring Modulith: `TenantMembershipReference`, `TenantUserAccessService`, `ProductUserAccess`, `ProductUserAccessService`, `IdentityUserLifecycleService` e expansão de `IdentityInvitationService`. Entidades/repositories reaproveitados e ajustados: `TenantMembership`, `TenantMembershipStatus`, `TenantMembershipRepository`, `ProductAssignment`, `ProductAssignmentStatus`, `ProductAssignmentRepository` e `KeycloakAdminClient`. Testes criados/alterados para mapper, service, controller, exception handler, APIs públicas de tenant/product/identity e client Keycloak.

**Endpoints confirmados:** `GET /api/v1/tenants/{tenantId}/users`; `POST /api/v1/tenants/{tenantId}/users/invite`; `GET /api/v1/tenants/{tenantId}/users/{userId}`; `PUT /api/v1/tenants/{tenantId}/users/{userId}`; `POST /api/v1/tenants/{tenantId}/users/{userId}/resend-invite`; `POST /api/v1/tenants/{tenantId}/users/{userId}/block`; `DELETE /api/v1/tenants/{tenantId}/users/{userId}`; `POST /api/v1/tenants/{tenantId}/users/{userId}/restore`. `DELETE` retorna `204 No Content`; erro de ultimo admin retorna HTTP 422 sem API deprecated no teste.

**Decisões de implementação registradas pelo GPT:** o domínio `users` foi implementado como orquestração sobre `TenantMembership`, `ProductAssignment` e Keycloak Admin API existente, sem autenticação paralela e sem entidade local duplicada de usuário. Para evitar ciclo modular, a API REST ficou no modulo `product.user`, consumindo apenas APIs públicas de `tenant`, `product` e `identity`. Os status persistidos continuam canônicos em portugues no contrato (`ativo`, `convidado`, `bloqueado`, `removido`), com aliases aceitos apenas na entrada de edição. O bloqueio nao desabilita a conta global no Keycloak; o soft delete desabilita no Keycloak somente quando o usuario nao possui membership ativa em outro tenant. O restore nao restaura `ProductAssignment`s automaticamente. A listagem por papel segue ADR-0019: `SUPER_ADMIN` e `TENANT_ADMIN` veem todos os usuarios do tenant; `PRODUCT_MANAGER`, `EDITOR` e `VIEWER` veem apenas usuarios que compartilham produto com o caller.

**Regras de negocio implementadas:** convite cria/resolve usuario no Keycloak via `KeycloakAdminClient`, cria membership local e assignments conforme produtos permitidos; e-mail duplicado no mesmo tenant retorna 409; detalhe/edicao respeitam isolamento por tenant; e-mail permanece imutavel; `resend-invite` exige usuario convidado; `block`, `DELETE` e mudanca de papel protegem o ultimo admin ativo; `DELETE` marca membership como `removido`, revoga assignments do tenant e preserva usuarios ainda ativos em outros tenants; `restore` aceita apenas `bloqueado` ou `removido`, reativa membership, reabilita Keycloak e dispara `UPDATE_PASSWORD`; usuario fora do escopo retorna 404, nunca 403.

**Bruno:** collection atualizada com a pasta `15-users`, cobrindo convite, duplicidade 409, listagem, detalhe, edicao, reenvio, bloqueio, restore de bloqueado, soft delete 204 e restore de removido. Variavel `tenantUserInviteEmail` adicionada em `collection.bru` e nos ambientes `local`, `dev`, `homolog` e `prod`.

**SonarQube for IDE:** apontamentos corrigidos sem `@SuppressWarnings`, falso positivo ou alteracao de contrato: `java:S5778` em lambdas de testes (`KeycloakAdminClientTest`, `TenantUserServiceTest`), `java:S1874` removendo uso deprecated de HTTP 422 (`TenantUserExceptionHandler`/`TenantUserControllerTest`), `java:S1192` centralizando literais de status em `TenantUserService`, e `java:S4144` removendo duplicacao de helpers de visibilidade nos testes.

**Retrofits pendentes para etapas futuras:** etapa 25/notification ainda deve integrar notificacoes internas reais para remocao/restauracao quando o dominio existir; etapa 29 deve substituir `executeActionsEmail(["UPDATE_PASSWORD"])` por tokens Aegis gerenciados para convite/restauracao/reset; auditoria persistente `USER_REMOVED_FROM_TENANT` e `USER_RESTORED_TO_TENANT` fica para o dominio `audit`; e-mails informativos de remocao/restauracao podem ser enriquecidos quando os templates definitivos estiverem disponiveis.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 809 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado. Teste direcionado `mvn -Dtest='TenantUserServiceTest,TenantUserControllerTest,KeycloakAdminClientTest' test` passou com `BUILD SUCCESS`, 57 testes, 0 falhas, 0 erros. Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: 121 requests executados, 121 aprovados, 231/231 testes aprovados. Evidencias completas: `results/sprint-15.md`.

## Etapa 16 — Dominio `audit` (concluida em 2026-06-27)

**Classes criadas/alteradas:** dominio `audit` completo — `AuditEvent` (com record interno `AuditEvent.Creation`), `AuditRisk`; `AuditEventRepository` (Javadoc completo, filtros combinaveis `actorSubject`/`productId`/`module`/`risk` via `@Query`); `AuditEventMapper` (MapStruct); `AuditEventSummary`, `AuditEventDetail`; `AuditRiskCatalog`, `AuditEventQueryService`; `AuditEventController`; `AuditEventNotFoundException`, `InvalidAuditRiskFilterException`, `AuditEventExceptionHandler`. API publica `audit.api`: `AuditService` (metodo **`recordEvent`**, nao `record` — ver Decisoes), `AuditRecordCommand`, `TenantVisibilityPort`. Migration `V8__audit_trail_normalization.sql` (renomeia/retipa colunas da tabela `audit_events` da V1, remove FKs `tenant_id`/`product_id`, adiciona `target_label`/`module`/`risk`/`trace_id`/`ip`/`user_agent`). Refatorados para usar `AuditService.recordEvent(...)` (nunca mais gravacao ad-hoc via `JdbcTemplate`): `TenantService` (+ `TENANT_CREATED`/`TENANT_UPDATED`, `updateTenant` ganhou `caller`), `TenantController`, `TenantAccessService` (passou a implementar `TenantVisibilityPort`), `TenantUserService` (+ `USER_INVITED_TO_TENANT`/`USER_BLOCKED` novos, `USER_REMOVED_FROM_TENANT`/`USER_RESTORED_TO_TENANT` refatorados — retrofit da Sprint 15 resolvido), `ProductAssignmentService`/`ProductAssignmentController` (+ `PRODUCT_ASSIGNMENT_CREATED`/`_REMOVED`, `assignUser`/`removeAssignment` ganharam `caller`), `ContentService` (+ `CONTENT_CREATED`/`CONTENT_PUBLISHED`), `AssetService`/`AssetController` (+ `ASSET_DELETED`, `deleteAsset` ganhou `caller`).

**Endpoints confirmados:** `GET /api/v1/tenants/{tenantId}/audit-events` (filtros `actorSubject`/`productId`/`module`/`risk`); `GET /api/v1/tenants/{tenantId}/audit-events/{eventId}`. Confirmado via teste automatizado e Bruno: nenhum `POST`/`PUT`/`DELETE` nesses paths.

**Decisoes de implementacao registradas pelo GPT:** (1) migration V8 e estritamente aditiva sobre a V1 (rename/retype via `ALTER TABLE`, mesmo padrao da V4); (2) FKs `tenant_id`/`product_id` removidas de `audit_events` deliberadamente (confirmado com o humano responsavel) — a trilha precisa sobreviver a exclusao do que audita, senao o proprio criterio de aceite ("excluir tenant, depois consultar audit-events daquele tenantId") nao seria satisfazivel; o codigo ad-hoc anterior contornava isso gravando `tenant_id = NULL`, um bug real corrigido nesta sprint; (3) isolamento do `AuditEventController` nunca verifica se o tenant existe em `tenants`, so SUPER_ADMIN ou membership ativa (decorrencia da decisao 2 — SUPER_ADMIN mantem acesso forense apos exclusao); (4) ciclo do Spring Modulith `audit <-> tenant` (audit precisa de `tenant.api` para isolamento, tenant precisa de `audit.api` para gravar) resolvido com Dependency Inversion: `TenantVisibilityPort` definido em `audit.api`, implementado por `TenantAccessService` (que ja mora em `tenant.api`) — `audit` nunca importa `tenant.api` diretamente; (5) `AuditService.record(...)` renomeado para `recordEvent(...)` a pedido explicito do humano responsavel numa rodada de correcao de SonarQube for IDE (`java:S6213`, identificador restrito) — diverge do nome literal do artefato, decisao tomada apos a implementacao inicial; (6) risco calculado por tabela literal em `AuditRiskCatalog` (12 acoes mapeadas, default `MEDIO` para acao desconhecida, nunca `BAIXO`); (7) `diffJson` e um envelope `{"before":..,"after":..}` montado só dentro de `AuditService`; (8) `traceId`/`ip`/`userAgent` ficam `null` nesta sprint (nenhum `Service` de dominio acessa `HttpServletRequest`, ver retrofit); (9) `actor`/`tenant`/`target` na resposta sao nomes resolvidos (via `IdentityUserDirectory`/`TenantVisibilityPort`/`targetLabel`), nunca IDs crus, com fallback gracioso quando a resolucao falha; (10) `id` adicionado ao `AuditEventSummary` alem dos 7 campos do mock do frontend, necessario para a listagem ser navegavel para o detalhe.

**Retrofits pendentes para etapas futuras:** popular `traceId`/`ip`/`userAgent` exige um filtro/interceptor de request dedicado (fora do escopo desta sprint, ver decisao 8); cenario Bruno "tenant fora do escopo (404)" usa o mesmo padrao de `nonMemberToken`/fallback 401 ja usado em `11-content` (`editorToken`) — sem seed automatico de segundo usuario nesta suite; module enable/disable (`ProductModuleService`) nao foi auditado nesta sprint (nao havia gravacao ad-hoc e nao esta na lista explicita do artefato).

**SonarQube for IDE:** 6 padroes reais corrigidos sem `@SuppressWarnings`/`NOSONAR`/desabilitacao de regra, todos via reescrita: `java:S6213` (rename `record`→`recordEvent`), `java:S1135` (falso gatilho na palavra "Todo" em portugues no Javadoc, reescrito), `java:S1168` (retornar `Map.of()` em vez de `null` em `readDiffJson`), `java:S7467` (`catch (IllegalArgumentException _)`), `java:S5778` (14 lambdas de teste com mais de uma chamada lancante, extraidas para variaveis locais antes do `assertThatThrownBy`), `java:S1192` (literal `"status"` extraido para `DIFF_KEY_STATUS` em `ContentService`/`TenantUserService`). Os 6 padroes foram documentados na nova Secao 4 de `AGENTS.md` a pedido do humano responsavel, para reduzir recorrencia.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 862 testes, 0 falhas, 0 erros, JaCoCo aprovado (100% linhas e branches, `All coverage checks have been met`) e Spring Modulith aprovado (incluindo a resolucao do ciclo `audit <-> tenant` via `TenantVisibilityPort`). Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: **133 requests executados, 133 aprovados, 253/253 testes aprovados**, confirmado em duas execucoes completas consecutivas (idempotente). Achado de plataforma documentado durante a validacao manual: `POST` num path que so tem `GET` mapeado retorna 403 `insufficient_scope` (com header `Allow: GET`) na aplicacao real, nao 405 — o filtro de excecoes do Spring Security intercepta a `HttpRequestMethodNotSupportedException` antes do handler de erro da aplicacao (mesma classe de quirk da Sprint 12); o teste `@WebMvcTest` isolado continua validando 405 corretamente. Evidencias completas: `results/sprint-16.md`.

## Etapa 17 — Dominios `settings` e `dashboard` (concluida em 2026-06-29)

**Classes criadas/alteradas:** dominio `settings` completo — `RolePermission` (entity, campos exatos do artefato: `id`/`tenantId`/`role`/`permissionKey`/`allowed`); `RolePermissionRepository` (Javadoc completo); `RolePermissionMapper` (MapStruct, `toSummary`); DTOs `RolePermissionSummary`, `RoleMatrixEntry` (shape unico reaproveitado por `roles` e `permission-matrix`), `SettingCard`; contracts `UpdateProductSettingsRequest`, `PermissionMatrixPreviewRequest`; `PermissionMatrixDefaults` (catalogo literal de fabrica, copiado de `frontend/src/core/permissions/roles.ts`); `SettingsService`; `SettingsController`; exececoes e `SettingsExceptionHandler`. Dominio `dashboard` — agregador puro, sem entidade: `DashboardSummaryResponse`, `DashboardService`, `DashboardController`. Migration `V9__settings_role_permissions.sql`. Toques cross-module (sinalizados antes da implementacao): `product.api.ProductAccessScope`/`ProductVisibilityService` (novos, expoem publicamente a mesma resolucao de papel de `ProductService.listProducts`, ADR-0019, sem duplicar logica), `product.api.ProductUserAccessService` (+`countDistinctAssignedUsers`/`countDistinctProductManagers`), `product.api.ProductReferenceService` (+`renameProduct`), `product.repository.ProductAssignmentRepository` (+`findAllByProductIdInAndStatus`), `submission.api.SubmissionAnalyticsService` (+`countToday`, metodo aditivo, sem alterar `SubmissionAnalyticsResponse` nem call-sites existentes da etapa 14).

**Endpoints confirmados:** `GET /api/v1/products/{productId}/settings/overview`; `PUT /api/v1/products/{productId}/settings`; `GET /api/v1/tenants/{tenantId}/roles`; `PUT /api/v1/tenants/{tenantId}/roles`; `POST /api/v1/tenants/{tenantId}/roles/restore-defaults`; `GET /api/v1/tenants/{tenantId}/permission-matrix`; `POST /api/v1/tenants/{tenantId}/permission-matrix/preview`; `POST /api/v1/tenants/{tenantId}/permission-matrix/restore-defaults`; `GET /api/v1/dashboard/summary` (sem path param, escopo resolvido pelo papel do caller no token). Nenhum endpoint extra; `POST /roles` de criacao de papel deliberadamente nao implementado (ver decisao 2).

**Decisoes de implementacao registradas pelo GPT:** (1) catalogo de `permissionKey` e a uniao literal dos paths ja usados em `roleVisibleNav`/`roleBlockedRoutePrefixes` do frontend (31 chaves, wildcards tratados como string opaca, nunca interpretados em runtime); (2) `POST /roles` (criar papel) nao foi implementado — o mock do frontend (`createRole`) sugeriria isso, mas ADR-0014 fixa catalogo fechado de 5 papeis e a lista oficial de endpoints da etapa nao inclui esse POST; (3) `GET /roles` e `GET /permission-matrix` retornam o mesmo shape `RoleMatrixEntry[]` (mesmo dado, duas telas); edicao só via `PUT /roles`; os dois `restore-defaults` chamam o mesmo metodo de servico; (4) matriz sempre devolve as 31 chaves para os 5 papeis (catalogo de fabrica como base, overrides do tenant por cima) — bug real corrigido durante a validacao manual: a versao inicial so usava o catalogo completo quando a tabela do tenant estava totalmente vazia, fazendo customizar uma unica chave esconder as outras 30; (5) `restoreDefaultPermissions` precisa de `flush()` explicito entre o delete e os inserts seguintes — bug real corrigido durante a validacao manual: o Hibernate ordena INSERTs antes de DELETEs no flush, violando a constraint unica ao recriar as mesmas linhas na mesma transacao (sintoma na aplicacao real: 403 `insufficient_scope`, mesma familia de quirk de traducao de excecao das Sprints 12/16, mas com causa raiz real e nova desta vez); (6) `PUT /products/{productId}/settings` cobre só o campo `name` (unico mutador real existente em `Product`) — nem o artefato nem o trace doc nem o mock do frontend definem um payload concreto para este endpoint; branding/SEO/publicacao ficam como retrofit; (7) `settings/overview` e majoritariamente um catalogo estatico de 8 cards — só o card "Permissoes" (padrao vs customizado) e o card "Produto" (status real) refletem dado real, os demais usam placeholder documentado; (8) `dashboard/summary`: `pendingContent = drafts`, `pendingContentNeedingReview = openApprovals = pendingReview` (mesmo numero, duas molduras, nao ha conceito de aprovacao separado ainda), `criticalApprovals = stalePendingReview`, `conversionRate` fixo em `"0%"` (sem tracking de trafego real, mesma decisao já tomada para `channels` na etapa 14); (9) escopo de `dashboard/summary` resolvido pelo mesmo filtro por papel de `ProductService.listProducts` (ADR-0019) via `ProductVisibilityService` — SUPER_ADMIN agrega globalmente, demais papeis agregam pelos produtos que conseguem ver; `activeUsers`/`productManagers` contam usuarios distintos com `ProductAssignment` ativo nesse mesmo conjunto de produtos, nao `TenantMembership`.

**Retrofits pendentes para etapas futuras:** nenhum outro dominio ainda le `RolePermission` para de fato gatear endpoints — a matriz e configuravel/consultavel, mas nao enforced em nenhum outro lugar do backend ainda; `PUT /products/{productId}/settings` cobre só `name`, branding/SEO/publicacao precisam de payload e schema definidos numa sprint futura; cards estaticos de `settings/overview` (Tenant, Equipe, Integracoes, Seguranca, Auditoria, SEO) podem ganhar dados reais numa sprint futura; `conversionRate` do dashboard fica fixo em `"0%"` até existir tracking de trafego real.

**SonarQube for IDE:** rodada de correcao pos-implementacao, todos via reescrita (sem `@SuppressWarnings`/`NOSONAR`): `java:S1192` em `PermissionMatrixDefaults` (31 chaves de permissao extraidas para constantes) e em `SettingsService` (`"SETTINGS"`/`"configurado"`/`"medio"`/`"alto"`/`"—"` extraidos); `java:S6862` em `ProductVisibilityServiceTest` (`OffsetDateTime.now()` do helper de teste trocado por timestamp fixo); `java:S4144` em `SettingsController` (`getRoles`/`getPermissionMatrix` e os dois `restore-defaults` deduplicados via metodos privados, mesma assinatura publica); `java:S6068`/`java:S5778` em `SettingsServiceTest` (matcher `eq` redundante removido; 3 lambdas de `assertThatThrownBy` com mais de uma chamada lancante corrigidas); `java:S3752` em `SpaFallbackController` (`@RequestMapping` generico trocado por `@GetMapping`, fallback de SPA só responde `GET`). Nenhum contrato, payload, migration ou regra de autorizacao alterado.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 932 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`, regra `BUNDLE`) e Spring Modulith aprovado (incluindo os novos modulos `settings`/`dashboard` e os metodos novos em `product.api`/`submission.api`). Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: **146 requests executados, 146 aprovados, 282/282 testes aprovados**, confirmado em duas execucoes completas consecutivas (idempotente) — a validacao manual contra a aplicacao real e o Postgres real revelou os dois bugs reais das decisoes 4 e 5, nao detectados pelos testes unitarios com mocks. Evidencias completas: `results/sprint-17.md`.

## Etapa 18 — Knowledge Graph: layout persistente, props ricas e nos orfaos (concluida em 2026-06-29)

**Classes criadas/alteradas:** `GraphNode` recebeu coordenadas persistentes `x`/`y`, reposicionamento e atualizacao controlada de `metadataJson`; `GraphNodeSummary`/`GraphNodeDetail` passaram a expor `type`, `status`, `x`, `y` e `props`; `GraphNodeMapper` passou a derivar props/status do metadata; `KnowledgeGraphService` e `KnowledgeGraphController` foram expandidos para layout, orfaos, preview e revisao de insights; adicionados `GraphInsightReview`, `GraphInsightReviewRepository`, `GraphNodePreview`, `GraphNodeProp`, `GraphInsightReviewSummary`, contratos da sprint e `InvalidGraphOrphanActionException`. API publica nova em `knowledgegraph.api`: `GraphNodeContentPreview` e `GraphNodeContentPreviewPort`, implementada por `ContentPreviewLookupService` em `content.api`; `ContentRepository` ganhou consulta por `productId` + `graphNodeId`. Migration `V10__knowledge_graph_layout_orphans.sql`.

**Endpoints confirmados:** `PATCH /api/v1/products/{productId}/graph/nodes/{nodeId}/position`; `GET /api/v1/products/{productId}/graph/orphans`; `POST /api/v1/products/{productId}/graph/orphans/{nodeId}/resolve`; `POST /api/v1/products/{productId}/graph/orphans/resolve`; `GET /api/v1/products/{productId}/graph/nodes/{nodeId}/preview`; `POST /api/v1/products/{productId}/graph/insights/review`.

**Decisoes de implementacao registradas pelo GPT:** a resolucao de orfaos e curatorial via metadata (`orphanResolved`, `orphanAction`, `status`), porque o contrato nao traz `targetNodeId` e portanto nao permite criar edge real sem inventar payload; `GET /graph/orphans` lista nodes sem edges e ainda acionaveis, filtrando resolvidos por metadata; preview de conteudo foi feito por porta definida em `knowledgegraph.api` e adapter em `content.api`, evitando ciclo Modulith `knowledgegraph -> content`; revisao de insight e idempotente por `product_id + text_hash`, com hash deterministico do texto normalizado.

**Bruno e documentacao:** Bruno atualizado com a pasta `18-knowledge-graph`, cobrindo criacao de orfaos, atualizacao de posicao, listagem, preview, resolucao individual/lote e revisao idempotente. `docs/api-testing/README.md` atualizado com a pasta 18 e variaveis `orphanNodeId`/`orphanBatchNodeId`/`reviewedInsightId`. `docs/trace/00_endpoints_esperados.md` atualizado com contratos e decisao de resolucao curatorial.

**SonarQube for IDE:** corrigido `java:S6809` em `KnowledgeGraphService`: `resolveOrphans(...)` chamava `resolveOrphan(...)` diretamente no mesmo bean, ambos transacionais; em Spring, self-invocation ignora o proxy transacional. A correcao manteve os dois metodos publicos como entradas transacionais e extraiu a operacao compartilhada para o helper privado `resolveOrphanNode(...)`, sem alterar contrato REST, regra de negocio, migration, payload, endpoints, Bruno ou comportamento funcional. Corrigido tambem `java:S5976` em `ContentPreviewLookupServiceTest`: tres testes exercitavam a mesma logica de preview nulo variando apenas metadata/difficulty e saida esperada; foram consolidados em um unico `@ParameterizedTest` com `@MethodSource`, preservando os tres cenarios nomeados e a cobertura. `AGENTS.md` atualizado com as recomendacoes preventivas de self-invocation transacional e testes parametrizados. Tambem foi removido um `@SuppressWarnings("unchecked")` legado em `ContentService` via `TypeReference<Map<String, Object>>`, sem supressao ou `NOSONAR`.

**Cobertura de testes:** `mvn -Dtest=ContentPreviewLookupServiceTest test` passou após a correcao `java:S5976`. `mvn clean verify` com `BUILD SUCCESS`, 967 testes, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado via suite completa. Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: **156 requests executados, 156 aprovados, 301/301 testes aprovados**, sem regressao na collection cumulativa. Evidencias completas: `results/sprint-18.md`.

## Etapa 19 — Read Model de Formulários, Submit Público e Configurações de Segurança (concluída em 2026-06-29)

**Classes criadas/alteradas:** `FormDefinition` recebeu `responseCount`, `lastActivityAt` e `registerResponse`; `FormDefinitionMapper`/`FormService` passaram a devolver `responses`, `lastActivity` relativo e `conversion` neutro. API pública `form.api.FormResponseReadModelService` atualiza o read model. Em `submission`, foram adicionados `SubmissionReceivedEvent`, publicação do evento em `SubmissionService`, `FormResponseCountListener`, `PublicSubmissionController`, `PublicSubmissionRateLimiter` e `TooManySubmissionsException`; `SubmissionExceptionHandler` passou a mapear submit inválido para 422 e rate limit para 429. Domínio `settings` ganhou `ProductSecuritySettings`, repository, mapper, service, controller, contrato e response para configurações de segurança. Migration `V11__forms_read_model_and_security_settings.sql`. `SecurityConfig` liberou `POST /api/v1/products/*/forms/*/submit`. `backend/pom.xml` ganhou encoding UTF-8 explícito.

**Endpoints confirmados:** `POST /api/v1/products/{productId}/forms/{formId}/submit`; `GET /api/v1/products/{productId}/settings/security`; `PUT /api/v1/products/{productId}/settings/security`.

**Decisões de implementação registradas pelo GPT:** o read model é atualizado por evento pós-commit, mas o listener ficou em `submission.service` chamando `form.api.FormResponseReadModelService`; a alternativa literal de colocar listener em `form` importando `submission.api` criava ciclo Modulith porque `submission` já depende de `form.api`. O listener usa `@TransactionalEventListener(AFTER_COMMIT)` e `REQUIRES_NEW`. `conversion` permanece `"—"` até existir tracking real. O submit público reutiliza `SubmitFormCommand`/`SubmissionService`, exige form publicado e campos obrigatórios respondidos, e aplica rate limit em memória de 10 submissões por IP/form/hora. O erro existente de form não publicado foi preservado como `SUBMISSION_FORM_NOT_PUBLISHED` para manter compatibilidade com a Sprint 13. `ProductSecuritySettings` é lazy-created no primeiro GET; `webhookSecret` é persistido/preservado, mas nunca retornado. `GET/PUT /settings/security` aceitam somente SUPER_ADMIN/TENANT_ADMIN; demais papéis recebem `SETTINGS_FORBIDDEN`.

**Regras de negócio implementadas:** submit público sem Authorization para formulário publicado; rejeição 422 para formulário não publicado e campo obrigatório ausente; incremento do read model somente após commit; rate limit 10/h por IP/form; segredo de webhook nunca serializado; status derivados de webhook, analytics e e-mail; analytics sem provider key retorna status de atenção.

**Bruno e documentação:** `bruno/13-forms` ganhou submit público feliz, form não publicado 422, campo obrigatório ausente 422 e listagem do read model após submit. Criada pasta `bruno/19-settings-security` com GET/PUT de security settings e analytics sem provider key. `docs/api-testing/README.md` atualizado com a pasta 19 e `publicSubmissionId`. Evidências detalhadas em `results/sprint-19.md`.

**SonarQube for IDE:** nenhuma supressão nova, `NOSONAR` ou marcador `TODO` foi adicionado. Corrigidos apontamentos finais reais: `java:S8692` (relógio real em testes), `java:S1068` (campo privado morto), `java:S5778` (lambdas de exceção com chamadas auxiliares) e `java:S6829` (`PublicSubmissionRateLimiter` como bean Spring stateful com construtor de injeção explícito via `@Autowired`). `AGENTS.md` atualizado com os padrões preventivos correspondentes. Observação: `SecurityConfig` já possuía `@SuppressWarnings({"java:S4502", "java:S112"})` antes desta sprint e não foi alterado.

**Retrofits pendentes para etapas futuras:** rate limit distribuído deve migrar para Redis/storage centralizado se o Aegis rodar com múltiplas instâncias; conversão real de formulários depende de tracking/analytics de tráfego; frontend ainda precisa trocar o path antigo `PUT /api/v1/admin/settings/security` por `PUT /api/v1/products/{productId}/settings/security` quando for ligado à API real.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 1001 testes, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado via suíte completa. Bruno validado contra a branch atual com `cd bruno && npx @usebruno/cli run --env local`: **163 requests executados, 163 aprovados, 318/318 testes aprovados**.

## Etapa 20 — Build do Frontend React servido pelo Spring Boot (SPA Same-Origin) (concluída em 2026-06-29)

**Classes/arquivos criados/alterados:** `SpaFallbackController` passou a servir o fallback SPA para rotas client-side em profundidade finita, com exclusão explícita de `/api/**`, `/actuator/**` e arquivos com extensão; `SecurityConfig` protege agora todo `/api/**` e permite rotas não-API necessárias ao shell React; `SpaFallbackControllerTest` cobre root, rota simples, rota profunda, API, actuator e asset. `frontend/vite.config.ts` voltou a gerar `frontend/dist`, enquanto `npm run build:backend` copia o build para `backend/src/main/resources/static` como artefato ignorado pelo git. Criada a pasta Bruno `20-spa-same-origin`; `docs/api-testing/README.md`, `.gitignore` e `AGENTS.md` foram atualizados.

**Rotas confirmadas:** `GET /`, `GET /dashboard`, `GET /products`, `GET /qualquer-rota` e rota profunda de cliente retornam o HTML da SPA; `GET /api/v1/me` permanece API protegida; `GET /actuator/health` permanece JSON do Actuator; assets como `/assets/index.js` não são capturados pelo fallback.

**Decisões de implementação registradas pelo GPT:** o fallback usa `@GetMapping` com padrões compatíveis com Spring PathPattern, excluindo primeiro segmento `api`/`actuator` e último segmento com ponto; não foi introduzido CORS nem hospedagem separada, preservando ADR-0009; `/api/**` foi protegido de forma ampla para evitar que futuras APIs fora de `/api/v1/**` sejam tratadas como rota pública; artefatos de build permanecem locais e fora do versionamento.

**SonarQube for IDE:** corrigido `java:S6856` no `SpaFallbackController` com `@PathVariable(name = "path", required = false)` e `@PathVariable(name = "segment", required = false)`, eliminando dependência de inferência por `-parameters`. `AGENTS.md` ganhou recomendação preventiva para binding explícito de toda variável de rota em controllers Spring MVC. Nenhum `@SuppressWarnings`, `NOSONAR` ou desativação de regra foi utilizado. A reanálise do SonarQube for IDE fica pendente na IDE local, pois não há runner CLI disponível neste ambiente.

**Bruno e documentação:** criada a pasta `20-spa-same-origin` cobrindo root SPA, rota client-side, `/api/v1/me` sem token válido e `/actuator/health`. O cenário `16-audit/sem-endpoint-de-escrita` foi alinhado para esperar 405 com `Allow: GET`, que é o comportamento real atual da aplicação. Evidências detalhadas em `results/sprint-20.md`.

**Cobertura de testes:** `npm audit` limpo (`found 0 vulnerabilities`) após upgrades sem major de `react-router` (`7.18.0`), `vite` (`6.4.3`) e `eslint` (`9.39.4`); `npm run typecheck` passou; `npm run lint` passou com 14 warnings preexistentes/não bloqueantes de Fast Refresh/hooks; `npm run build:backend` passou com Vite `6.4.3`. `mvn clean verify` com `BUILD SUCCESS`, 1003 testes, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado via suíte completa. Bruno validado contra a branch atual com `cd bruno && npx @usebruno/cli run --env local`: **167 requests executados, 167 aprovados, 327/327 testes aprovados**.

## Etapa 21 — Dockerfile do Backend e Docker Compose Completo (concluída em 2026-06-29)

**Classes/arquivos criados/alterados:** nenhuma classe Java foi criada ou alterada. `backend/Dockerfile` passou a usar build multi-stage com stage Node para gerar a SPA, Java 25 para o backend (`maven:3.9-eclipse-temurin-25` -> `eclipse-temurin:25-jre`) e empacotamento do JAR executável com `spring-boot:repackage`; `.dockerignore` foi criado para o contexto raiz. `docker-compose.yml` ganhou o serviço `aegis-backend` com build local (`context: .`, `dockerfile: backend/Dockerfile`), `restart: unless-stopped`, rede `aegis-network`, dependências para PostgreSQL/Keycloak/MailHog, variáveis de banco/Keycloak/SMTP/storage/multipart, porta `${BACKEND_PORT}:8080`, volume persistente `aegis_assets_data:/app/assets` e bind read-only dos templates de e-mail de produto. `KC_HOSTNAME` do Keycloak foi configurado para preservar o issuer externo. Bruno ganhou a pasta `21-docker-stack`; `.env.example`, `docs/api-testing/README.md`, `docs/deploy/README.md` e este consolidado foram atualizados.

**Rotas e integrações confirmadas:** `GET /actuator/health` no backend containerizado retornou `status: UP`; `GET http://localhost:8282/realms/aegis/.well-known/openid-configuration` retornou issuer `http://localhost:8282/realms/aegis`; token emitido pelo Keycloak externo validou com sucesso em `GET /api/v1/me` no backend containerizado, retornando o usuário `loki` com role `super_admin`.

**Decisões de implementação registradas pelo GPT:** a imagem final do backend contém apenas JRE e JAR, sem Maven, Node, código fonte ou dependências de build; o Dockerfile gera `frontend/dist` dentro de um stage Node e copia a SPA para `src/main/resources/static` antes do Maven, então `docker compose up -d --build` funciona em checkout limpo sem depender de artefato estático pregerado; o issuer externo continua sendo `http://localhost:8282/realms/aegis`, enquanto o backend usa `SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI=http://keycloak:8080/.../certs` e `KEYCLOAK_INTERNAL_BASE_URL=http://keycloak:8080` apenas para comunicação container-to-container; o storage local mantém o bootstrap Java com `Files.createDirectories(...)`, mas no container a raiz passa a ser `/app/assets` sobre o volume nomeado `aegis_assets_data`.

**Persistência validada:** após `docker compose down && docker compose up -d` sem `-v`, os dados sobreviveram nos três pontos obrigatórios: marcador de assets em `/app/assets/aegis/pms/sprint-21/marker.txt`, PostgreSQL Aegis com `flyway_schema_history` em contagem `11`, e PostgreSQL Keycloak com `realm` em contagem `2`. Volumes confirmados: `aegis-pms_aegis_assets_data`, `aegis-pms_aegis_postgres_data` e `aegis-pms_keycloak_aegis_postgres_data`.

**Bugs/ajustes reais encontrados:** a primeira imagem Docker gerou JAR sem manifesto executável para `java -jar`, corrigido com `spring-boot:repackage`; a porta 8080 estava ocupada por uma execução local do backend via IntelliJ e foi liberada para validar a porta oficial do compose; o teste Bruno inicial do discovery usava `bru.getVar("keycloakIssuer")`, mas a CLI não expôs essa variável no bloco de teste apesar de interpolar a URL, então o teste passou a validar explicitamente o issuer externo esperado. O MailHog em host arm64 emitiu warning de plataforma `linux/amd64`, sem impacto funcional.

**Bruno e documentação:** criada a pasta `21-docker-stack` com `backend-health-containerizado` e `keycloak-discovery-issuer`. `docs/deploy/README.md` passou a documentar o fluxo oficial `docker compose up -d --build`, serviços e volumes persistentes. `docs/api-testing/README.md` registra a pasta 21 e informa que a sprint usa `baseUrl`/`keycloakIssuer`.

**SonarQube for IDE:** nenhum código Java foi alterado; nenhuma supressão, `NOSONAR` ou desativação de regra foi adicionada. A reanálise visual do SonarQube for IDE fica pendente na IDE local, pois não há runner CLI disponível neste ambiente.

**Cobertura de testes:** `docker compose config` aprovado sem warnings; `docker build -t aegis-backend -f backend/Dockerfile .` aprovado; `docker compose up -d --build` subiu `aegis-backend`, `aegis-keycloak`, `aegis-mailhog`, `aegis-postgres` e `keycloak-postgres-aegis`; `mvn clean verify` com `BUILD SUCCESS`, 1003 testes, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado via suíte completa; Bruno validado contra a stack containerizada com `cd bruno && npx @usebruno/cli run --env local`: **169 requests executados, 169 aprovados, 331/331 testes aprovados**. Evidências completas: `results/sprint-21.md`.

## Etapa 22 — Seed inicial e Knowledge Graph (concluída em 2026-06-29)

**Classes/arquivos criados/alterados:** criado o módulo `seed` com `DemoSeedRunner` e `DemoSeedService`, ativo somente no profile `local`. Foram adicionadas APIs públicas de seed em `tenant.api`, `product.api`, `knowledgegraph.api` e `identity.api`: `TenantSeedCommand`, `TenantSeedReference`, `TenantSeedService`, `ProductSeedCommand`, `ProductSeedReference`, `ProductSeedService`, `GraphSeedNodeCommand`, `GraphSeedEdgeCommand`, `GraphSeedNodeReference`, `KnowledgeGraphSeedService` e `IdentityDemoUserService`. `KeycloakAdminClient` ganhou `ensureDemoUser(...)` para criar/atualizar usuário demo no Keycloak, habilitar conta, redefinir senha permanente e associar role de realm. Testes unitários cobrem os services de seed, runner local e fluxos novos do Keycloak Admin Client. Bruno ganhou a pasta `22-seed-inicial-e-grafo`; `docs/api-testing/README.md`, `docs/sprints/backend/22_seed_inicial_e_grafo.md`, este consolidado e `results/sprint-22.md` foram atualizados.

**Endpoints confirmados:** nenhum endpoint REST novo foi criado nesta sprint; a validação confirmou os endpoints existentes usados pelo seed e pela demonstração: `POST /api/v1/auth/login`, `GET /api/v1/tenants`, `GET /api/v1/products`, `GET /api/v1/products/{productId}/graph/nodes?q=...`, `GET /api/v1/products/{productId}/graph/nodes/{nodeId}/neighbors` e `GET /api/v1/products/{productId}/graph/nodes/{nodeId}/related`.

**Dados seedados:** tenants `clientes-beta` (`CLIENTES BETA`, Pro, ativo), `aegis-labs` (Enterprise, ativo) e `cliente-norte` (Starter, suspenso); produtos `maestro-beton`, `conecta-talentos`, `alexandre-dev`, `cmss`, `loki`, `wikidev`, `aegis-core`, `aegis-docs` e `portal-norte`, com módulos sincronizados conforme cada produto; usuários demo `super-admin@byop.io`, `admin@byop.io`, `pm@byop.io`, `editor@byop.io` e `viewer@byop.io`, todos com senha local `senha123`; memberships e assignments coerentes para os papéis globais. O super-admin recebeu assignments em todos os produtos seedados porque o acesso operacional aos domínios por produto continua passando por `ProductAccessResolver`/ADR-0018.

**Knowledge Graph confirmado:** WikiDev foi seedado com os nós WikiDev, Programacao, Java, Spring Boot e JPA, ligados por `CONTAINS` e `RELATED_TO`; Loki foi seedado com Loki, Poema do Limiar, Musica Ecos e Playlist Atravessias, ligados por `CONTAINS`, `INSPIRED_BY` e `PART_OF`. A playlist foi representada como `GraphNodeType.CONTENT` com metadata `refType=PLAYLIST`, porque o enum atual do grafo não possui `PLAYLIST` e a sprint não ampliou esse contrato. Foi adicionada uma aresta Loki -> Poema do Limiar para manter o produto Loki conectado no grafo demo.

**Decisões de implementação registradas pelo GPT:** o seed é idempotente por chaves naturais (`tenant.key`, `product.key`, refs de node/edge e e-mail de usuário), roda apenas em `local` e não cria autenticação própria; a orquestração em `seed` consome somente APIs públicas expostas por `NamedInterface`, preservando a auditoria Spring Modulith; o tenant de demonstração principal foi `CLIENTES BETA`, sem criar um tenant técnico `BYOP`; o cabeçalho do artefato da sprint foi corrigido de "Etapa 21" para "Etapa 22" conforme o nome real do arquivo. O prompt mencionava `README.md` na raiz e `docs/sprints/backend/00_endpoints_esperados.md`, mas esses arquivos não existem; foram usadas as fontes reais `docs/README.md` e `docs/trace/00_endpoints_esperados.md`.

**Bruno e documentação:** `bruno/22-seed-inicial-e-grafo` cobre login do super-admin demo, listagem dos tenants/produtos seedados, busca de nodes Spring Boot/JPA/Poema/Musica e navegação de neighbors/related em WikiDev e Loki. `docs/api-testing/README.md` registra a pasta 22 e as variáveis de IDs seedados. Evidências detalhadas ficam em `results/sprint-22.md`.

**SonarQube for IDE:** rodada pós-implementação corrigiu os apontamentos do `DemoSeedService.java`, sem supressão, `NOSONAR`, desativação de regra ou falso positivo. `java:S2068` foi resolvido removendo a constante sensível `PASSWORD`; a credencial demo agora vem da property local `aegis.seed.demo-user-initial-credential` em `application-local.yml`, com default `senha123` apenas para desenvolvimento local. `DemoSeedService` e `DemoSeedRunner` ficam restritos ao profile `local`, então o seed não executa em `prod`. `java:S1192` foi resolvido extraindo slugs repetidos de usuários e produtos para constantes semânticas (`USER_SUPER_ADMIN_SLUG`, `USER_EDITOR_SLUG`, `USER_VIEWER_SLUG`, `PRODUCT_MAESTRO_BETON`, `PRODUCT_ALEXANDRE_DEV`, etc.). `AGENTS.md` foi atualizado para prevenir reincidência em seeds futuros. A reanálise visual do SonarQube for IDE fica pendente na IDE local, pois não há runner CLI disponível neste ambiente.

**Retrofits pendentes para etapas futuras:** quando a etapa 25/notification existir, avaliar o fan-out de notificação `ONBOARDING` para os 5 usuários demo, conforme observação do artefato da Sprint 22. Se o enum de `GraphNodeType` ganhar um tipo `PLAYLIST`, reavaliar a playlist do Loki hoje modelada como `CONTENT` + metadata `refType=PLAYLIST`.

**Cobertura de testes:** teste direcionado de seed `mvn -Dtest='*Seed*Test,*Demo*Seed*Test,*KnowledgeGraph*Seed*Test' test` com `BUILD SUCCESS`, 21 testes, 0 falhas, 0 erros e 0 ignorados. `mvn clean verify` com `BUILD SUCCESS`, 1029 testes, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado via suíte completa. Bruno validado contra a aplicação local com profile `local`: **180 requests executados, 180 aprovados, 354/354 testes aprovados**. Idempotência confirmada após segunda subida e novamente após a correção Sonar: 3 tenants, 9 produtos, 9 nós WikiDev/Loki e 7 arestas WikiDev/Loki, sem duplicação.

## Etapa 23 — Domínio `pages`, seções e blocos (concluída em 2026-07-02)

**Classes criadas/alteradas:** módulo `br.com.byop.aegis.pages` completo — entidades `Page`, `PageSection`, `ProductGlobals`, `Event`; enums `PageStatus`, `BlockType`, `EventAccessType`, `EventVisibility`; catálogo genérico `BlockTypeDefinition`/`BlockTypeCatalog` (mecanismo `acceptsChildren` orientado a dados, sem `if`/`switch` por tipo); repositórios `PageRepository`, `PageSectionRepository` (com `existsByFormIdReference`, query JSONB nativa), `ProductGlobalsRepository`, `EventRepository` (Javadoc completo); mappers MapStruct `PageMapper`, `PageSectionMapper`, `ProductGlobalsMapper`, `EventMapper`; `SectionContentValidationService` (classe mais crítica — `validateSectionContent`/`sanitizeContent`) e `PageMarkdownSanitizer` (cópia renomeada do sanitizador da Sprint 11, ver decisões); `PageService`, `ProductGlobalsService`, `EventService` (auditoria via `AuditService.recordEvent` em toda mutação); `PageController` (`@RequireModule(ModuleKey.PAGES)`), `EventController` (mesmo módulo `PAGES`), `ProductGlobalsController` (sem module-gating); `PageExceptionHandler` + 10 exceções de domínio. Migration `V12__pages_sections_globals_events.sql`. Extensão aditiva em `asset.api.AssetReference`/`AssetReferenceService` (campo `category`, string de contrato, não o enum interno) para permitir validar categoria de asset nos blocos `audio`/`video`/`download` sem violar o Spring Modulith.

**Endpoints confirmados:** `GET/POST /api/v1/products/{productId}/pages`; `GET/PUT/DELETE /api/v1/products/{productId}/pages/{pageId}`; `POST /api/v1/products/{productId}/pages/{pageId}/sections`; `PUT/DELETE .../sections/{sectionId}`; `PUT .../sections/reorder`; `GET/PUT /api/v1/products/{productId}/globals`; `GET/POST /api/v1/products/{productId}/events`; `GET/PUT/DELETE /api/v1/products/{productId}/events/{eventId}` — exatamente os endpoints do artefato, sem divergência. Module-gating: `PageController`/`EventController` com `@RequireModule(ModuleKey.PAGES)` (módulo já existia no catálogo); `ProductGlobalsController` deliberadamente sem module-gating (estrutura básica do produto).

**Decisões de implementação registradas pelo GPT:** (1) `Event` implementado dentro do próprio módulo `pages` (não um módulo top-level novo) — única dependência cruzada é o bloco `event-list` referenciando `Event`, sem necessidade de `NamedInterface` extra; (2) catálogo `acceptsChildren` de `two-column` usa `cta-section` em vez do literal `cta` do artefato — `"cta"` nunca existiu no catálogo fechado de `BlockType` (Seção B), então mantê-lo tornaria essa entrada da lista permanentemente inalcançável (qualquer seção `type: "cta"` já seria rejeitada por `UNKNOWN_BLOCK_TYPE` antes de chegar na checagem de filhos); (3) `Event.datetime` modelado como `LocalDateTime` (não `OffsetDateTime`) — corresponde ao valor sem timezone que o artefato exemplifica (`2026-07-12T16:00`); (4) `pages`/`sections`/`globals`/`events` chamam `AuditService.recordEvent` em toda mutação, seguindo o padrão de `content`/`asset` (não o de `form`, que nunca chamou auditoria — inconsistência preexistente registrada, não corrigida por estar fora de escopo); (5) `ProductGlobals` ganhou `createdAt` interno (não pedido pelo artefato, que só lista `updatedAt`) para cumprir a Constituição (Artigo X) sem alterar o contrato REST (campo nunca serializado); (6) `slug` incluído em `UpdatePageRequest` para suportar o cenário "mudar o slug de uma página publicada" citado na Seção E, validado contra colisão com outra página do mesmo produto; (7) `PageSectionRepository.existsByFormIdReference` usa query JSONB nativa (`content_json ->> 'formId'`) em vez de coluna dedicada, evitando alterar o schema além do pedido.

**Bugs reais de plataforma encontrados e corrigidos durante a validação manual com a aplicação real:** (1) duas classes homônimas `MarkdownSanitizer` (uma em `content.service`, outra recém-criada em `pages.service`) quebraram o boot real da aplicação com `ConflictingBeanDefinitionException` — o component-scan do Spring Boot gera o nome do bean a partir do simple name da classe, ignorando o pacote; não detectado pelos testes `@WebMvcTest`/Mockito (que nunca sobem o contexto Spring completo), só na validação manual via `mvn spring-boot:run`. Corrigido renomeando a cópia do domínio `pages` para `PageMarkdownSanitizer`. (2) a pasta `22-seed-inicial-e-grafo` termina autenticada como o usuário demo `super-admin@byop.io` (sem `ProductAssignment` no `{{productId}}` avulso criado em `02-products`), fazendo toda a pasta `23` falhar com 403 `PRODUCT_CONTENT_ACCESS_DENIED`; corrigido adicionando `relogar-como-loki` como primeiro request da pasta 23.

**Rodada de correção SonarQube for IDE (pós-implementação):** 5 apontamentos reais corrigidos em `PageService`, `PageServiceTest`, `ProductGlobalsServiceTest`, `SectionContentValidationService` e `SectionContentValidationServiceTest`, todos via reescrita, nenhum via supressão — `java:S1168` (nunca retornar `null` de método `Map`, corrigido sem alterar payload/persistência movendo a decisão de nulidade para o call site); `java:S1135` (apontamento real desta vez — "Todo" abria a primeira frase de um Javadoc); `java:S5778` (19 ocorrências — `Map.of(...)`/`List.of(...)` inline dentro da lambda de `assertThatThrownBy` contam como segunda invocação, extraídas para variável local); `java:S5838` (`assertThat(map).containsEntry(key, value)` em vez de `assertThat(map.get(key)).isEqualTo(value)`); `java:S5976` (3 testes de `href` inválida do bloco `social-links` consolidados em um `@ParameterizedTest`/`@MethodSource`). `AGENTS.md` atualizado (Seções 4 e 6) para prevenir recorrência.

**Retrofits pendentes para etapas futuras:** `form` (etapa 13) ainda não chama `AuditService` (inconsistência preexistente, fora de escopo aqui); nenhuma integração com Knowledge Graph criada para `Page`/`Event` (o artefato não pede — `GraphNodeType.PAGE`/`EVENT` já existem no catálogo desde a etapa 08, caso uma sprint futura decida conectar); redirecionamento ao trocar `slug` de página publicada é explicitamente fora de escopo (só a checagem de colisão foi implementada).

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, **1230 testes**, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`, 100% linhas e branches) e Spring Modulith aprovado (`ModulithArchitectureTest`, incluindo a nova fronteira `pages` consumindo apenas `asset.api`/`form.api`). `PageRepository`/`PageSectionRepository`/`ProductGlobalsRepository`/`EventRepository` com Javadoc completo. `SectionContentValidationServiceTest`: 68 cenários, cada um dos 13 `BlockType` com regra específica cobrindo pelo menos 1 caminho válido e 1 de rejeição, mecanismo `acceptsChildren` com 6 cenários dedicados. Bruno validado via `cd bruno && npx @usebruno/cli run --env local`: **218 requests executados, 218 aprovados, 427/427 testes aprovados**, confirmado em múltiplas execuções consecutivas (idempotente) antes e depois da rodada Sonar, sem regressão nas pastas 00–22. Evidências completas: `results/sprint-23.md`.

## Etapa 24 — OpenAPI/Swagger, testes e checklist final (concluída em 2026-07-02)

**Objetivo:** habilitar OpenAPI/Swagger em `local` e `dev`, manter Swagger/OpenAPI desabilitado em `prod`, expor Bearer JWT na UI, organizar tags por domínio na ordem canônica e fechar a etapa com testes, Bruno e checklist de qualidade.

**Classes criadas:** `OpenApiConfig` em `br.com.byop.aegis.api.openapi` e `OpenApiConfigTest`.

**Classes alteradas:** nenhuma classe existente foi alterada; a sprint adicionou uma nova configuração OpenAPI e teste dedicado.

**Arquivos criados/alterados:** criado `OpenApiConfig`, `OpenApiConfigTest`, `application-dev.yml`, a pasta Bruno `24-openapi-testes-checklist-final` (`folder.bru`, `swagger-ui-local.bru`, `openapi-json-completo.bru`) e `results/sprint-24.md`. Alterados `application.yml`, `application-local.yml`, `application-prod.yml`, `docs/api-testing/README.md`, `AGENTS.md`, `00_padrao_qualidade_e_arquitetura.md` e este `SPRINT-RESULTADO.md`.

**Endpoints e contratos OpenAPI/Swagger confirmados:** `GET /swagger-ui/index.html` retorna a UI Swagger em profile `local`; `GET /v3/api-docs` retorna a especificação OpenAPI com metadata, servidor local, security scheme Bearer JWT, tags ordenadas e operações documentadas. Em `prod`, os beans OpenAPI não são criados e as properties Springdoc permanecem desabilitadas. Bruno confirma que não há operação sem `summary`, `description` ou `responses`.

**Decisões de implementação registradas pelo GPT:** Swagger/OpenAPI foi tratado como configuração de ambiente, combinando `@Profile({"local", "dev"})` com properties Springdoc por profile para evitar exposição acidental em produção. Tags OpenAPI viraram constantes semânticas (`TAG_PRODUCTS`, `TAG_AUTH`, etc.) e a lista fixa foi centralizada em `ORDERED_TAGS`/`orderedTagNames()`, permitindo que os testes validem a ordem sem duplicar o catálogo. O customizador classifica operações por path, mantendo endpoints públicos (`System`, `Auth` e submissão pública) sem requirement Bearer e endpoints administrativos com Bearer JWT. Nenhum controller, payload, migration, endpoint REST, profile de negócio ou regra de autorização foi alterado.

**SonarQube for IDE:** rodada dedicada corrigiu os apontamentos de `OpenApiConfig.java` sem `@SuppressWarnings`, `NOSONAR`, desativação de regra ou falso positivo: `java:S1192` (tags OpenAPI duplicadas extraídas para constantes semânticas) e `java:S3923` (condicional de status HTTP com retorno igual nos dois ramos removida). `AGENTS.md` e `00_padrao_qualidade_e_arquitetura.md` registram a prevenção futura: tags OpenAPI centralizadas, lista fixa única, sem condicionais redundantes e testes por profile quando OpenAPI depender de ambiente. Nenhum bug funcional novo foi encontrado; a rodada Sonar foi tratada como correção estrutural sem alteração de contrato.

**Bruno e documentação:** pasta `24-openapi-testes-checklist-final` criada com `swagger-ui-local` e `openapi-json-completo`, ambos sem autenticação, validando a UI e o JSON OpenAPI. `docs/api-testing/README.md` registra a pasta 24 e informa que ela não cria variáveis novas. Evidências completas em `results/sprint-24.md`.

**Cobertura de testes e critérios de aceite atendidos:** `mvn clean test` com `BUILD SUCCESS`, **1238 testes**, 0 falhas, 0 erros e 0 ignorados. Teste direcionado `mvn -Dtest=OpenApiConfigTest test` com `BUILD SUCCESS`, 8 testes, 0 falhas e 0 erros. `mvn clean verify` com `BUILD SUCCESS`, **1238 testes**, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado (`ModulithArchitectureTest`, 1 teste, 0 falhas, 0 erros). Bruno validado contra a aplicação local: pasta 24 com **2 requests executados, 2 aprovados, 7/7 testes aprovados**; collection cumulativa com **220 requests executados, 220 aprovados, 434/434 testes aprovados**. O grep obrigatório `grep -rn "@SuppressWarnings\|NOSONAR" src/main src/test` encontrou apenas duas ocorrências preexistentes fora do escopo (`SecurityConfig` e `AuditEventQueryService`), nenhuma nova ocorrência adicionada nesta sprint. `grep -rn "@MockBean\|@SpyBean" src/test` não retornou ocorrências. Swagger segue funcionando em `local/dev`, bloqueado em `prod`, com Bearer JWT e tags ordenadas.

## Etapa 25 — Dominio `notification` (concluida em 2026-07-02)

**Classes criadas/alteradas:** modulo `br.com.byop.aegis.notification` completo com `Notification`, `UserNotificationStatus`, `NotificationType`, `NotificationPresentationMode`, repositories com Javadoc, contracts/DTOs `CreateNotificationRequest`, `NotificationResponse`, `NotificationWithStatus`, mapper MapStruct `NotificationMapper`, `NotificationService`, `NotificationController`, excecoes e `NotificationExceptionHandler`. API publica `notification.api.NotificationOnboardingService` exposta por `NamedInterface`. Criado `shared.markdown.SharedMarkdownSanitizer` exposto como `@NamedInterface("markdown")`; `content.service.MarkdownSanitizer` e `pages.service.PageMarkdownSanitizer` passaram a delegar para ele. Alterados `TenantMembershipRepository`, `TenantUserAccessService`, `TenantUserService` e `DemoSeedService` para resolver users ativos e atribuir onboarding no convite/seed local. Migration `V13__notifications.sql` cria `notifications`, `user_notification_statuses`, constraints, indices e a notificacao tecnica `ONBOARDING`.

**Endpoints confirmados:** `GET /api/v1/notifications/mine`; `GET /api/v1/notifications/mine/pending-modal`; `POST /api/v1/notifications/{notificationId}/mark-shown`; `POST /api/v1/notifications/{notificationId}/mark-read`; `POST /api/v1/notifications` (SUPER_ADMIN); `GET /api/v1/notifications` (SUPER_ADMIN).

**Decisoes de implementacao registradas pelo GPT:** fan-out materializado no momento da criacao, com uma linha de `UserNotificationStatus` por destinatario e constraint unica `(notification_id, user_subject)`. Target `ALL` usa subjects distintos com membership ativa em qualquer tenant; target `TENANT` limita ao tenant informado; target `USERS` valida todos os subjects via `IdentityUserDirectory` antes de persistir qualquer status, evitando fan-out parcial. Onboarding e uma notificacao `ONBOARDING` tecnica criada por Flyway; o fan-out dos usuarios demo fica no seed local porque o subject real do Keycloak e dinamico, e usuarios convidados recebem onboarding via `NotificationOnboardingService.assignOnboarding(...)`. Sanitizacao de markdown foi extraida para `shared.markdown` para evitar dependencia de `pages` em classe interna de `content`; o Javadoc de `PageMarkdownSanitizer` foi ajustado para referenciar o sanitizer compartilhado. `NotificationExceptionHandler` ficou restrito a `NotificationController`, impedindo vazamento de tratamento de excecoes para outros modulos.

**Bugs/ajustes reais encontrados na validacao com aplicacao real:** queries derivadas de `UserNotificationStatusRepository` usando `notificationId` falharam no boot porque `notificationId` nao e propriedade JPA direta; corrigido com JPQL explicito sobre `status.notification.id` e `@Param`. Criacao de notificacao `ALL` falhou inicialmente por parametros JPQL nomeados sem `@Param` em `TenantMembershipRepository`; corrigido e coberto por teste. Bruno da pasta 25 precisou usar `formUrlEncoded` no login e senhas demo `senha123`; a pasta 24 foi ajustada para esperar a nova tag OpenAPI `Notifications`.

**SonarQube for IDE:** rodada final corrigiu 12 apontamentos sem `@SuppressWarnings`, `NOSONAR`, desativacao de regra ou falso positivo: `java:S6829` em `MarkdownSanitizer` e `PageMarkdownSanitizer` com `@Autowired` explicito no construtor de injecao; `java:S7467` em `NotificationService` com unnamed pattern no `catch`; `java:S5778` em `NotificationServiceTest` extraindo helpers das lambdas de `assertThatThrownBy`; `java:S5976` consolidando cenarios repetidos de target invalido em `@ParameterizedTest`/`@MethodSource`.

**Bruno e documentacao:** criada a pasta `25-notifications`, cobrindo login super admin, criacao `ALL`, rejeicao de usuario inexistente em `USERS`, notificacao `BELL_ONLY`, pending modal, `mark-shown`, `mark-read`, 403 para editor em endpoints admin e convite de usuario com onboarding. `docs/api-testing/README.md`, sprint docs, ambientes Bruno e `results/sprint-25.md` foram atualizados; os arquivos de sprint posteriores foram renumerados/corrigidos para preservar a sequencia 25, 26, 27, 28 e 29.

**Retrofits pendentes para etapas futuras:** notificacoes internas para remocao/restauracao de usuario continuam como evolucao de produto; a sprint integrou onboarding no convite, mas nao alterou os fluxos informativos de remocao/restauracao da etapa 15. A etapa 29 ainda deve substituir `executeActionsEmail(["UPDATE_PASSWORD"])` por tokens Aegis gerenciados.

**Cobertura de testes:** teste direcionado apos a rodada Sonar `mvn -Dtest='NotificationServiceTest,SharedMarkdownSanitizerTest,MarkdownSanitizerTest,PageMarkdownSanitizerTest' test` com `BUILD SUCCESS`, 44 testes, 0 falhas, 0 erros. `mvn clean verify` com `BUILD SUCCESS`, **1285 testes**, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado pela suite completa. Bruno validado contra aplicacao real em profile `local`: **236 requests executados, 236 aprovados, 459/459 testes aprovados**. Evidencias completas: `results/sprint-25.md`.

## Etapa 26 — Templates de produto: esqueleto de paginas no `POST /products` (concluida em 2026-07-03)

**Classes criadas/alteradas:** `product.domain.ProductModuleTemplateCatalog` (catalogo fixo de modulos recomendados por `ProductTypeKey`, ordem ja resolve dependencia `CONTENT` antes de `KNOWLEDGE_GRAPH`); `pages.domain.BlockDefaults` (conteudo default vazio/minimo por `BlockType`); `pages.domain.PageTemplateCatalog` + `PageSkeletonTemplate`/`SectionSkeletonTemplate` (esqueleto de paginas/secoes por tipo de produto, chaveado por `String` — nunca `ProductTypeKey` — para nao criar dependencia de `pages` em `product.domain`); `pages.service.ProductPageScaffoldService` (cria o esqueleto reagindo a `ProductCreatedEvent`). Alterados `product.api.ProductCreatedEvent` (campos novos `productType`/`defaultLocale`), `product.service.ProductService` (habilita modulos recomendados e publica o evento estendido), `tenant.service.TenantService` (publica `tenant.api.TenantStatusChangedEvent`/`TenantLifecycleTransition`, novos, so nas transicoes `ACTIVE↔SUSPENDED`), `notification.service.NotificationService` (`notifyTenantStatusChange` + listener `onTenantStatusChanged`) e `pages.service.SectionContentValidationService` (`validateFormReference`/`validateDownload` relaxados — ver decisao abaixo).

**Endpoints confirmados:** nenhum endpoint novo — o scaffold e a notificacao sao efeito colateral de `POST /api/v1/products` e `PUT /api/v1/tenants/{tenantId}`, ambos ja existentes.

**Decisoes de implementacao registradas pelo GPT:** (1) duas fronteiras de modulo que seriam ciclicas se chamadas direto (`product→pages`, `tenant→notification`, ja que `pages` depende de `product.api` e `notification` depende de `tenant.api`) foram resolvidas com o mesmo padrao de evento ja usado por `asset` na Sprint 12 (`ProductCreatedEvent` estendido; `TenantStatusChangedEvent` novo). (2) Diferente do listener de `asset` (`AFTER_COMMIT`, side effect em disco), os dois listeners novos usam `@TransactionalEventListener(phase = BEFORE_COMMIT)` porque a etapa exige atomicidade real com o banco — uma falha no listener ainda roda dentro da mesma transacao fisica do `Product`/`Tenant` e forca rollback. (3) `Workflow` (modulo recomendado de `Produto SaaS` na ADR-0017) nao existe no catalogo fechado `ModuleKey` — nao foi inventado um valor de enum novo; `Produto SaaS` habilita so os 5 modulos existentes, `WORKFLOW` fica retrofit pendente (mesmo tratamento ja dado a `ECOMMERCE`). (4) `validateFormReference`/`validateDownload` (Sprint 23) foram relaxados para aceitar `formId`/`items` ausentes ou vazios como estado "ainda nao configurado" — sem isso, os blocos `contact` (pagina "contato") e `download` (bloco "Downloads" do Portfolio) nunca poderiam nascer vazios, ja que a validacao original exigia sempre uma referencia real a um Form/Asset existente; `formId` malformado ou apontando para entidade inexistente continua rejeitado como antes. (5) `event-list` usa `{title, source: {}}` em vez da forma antiga do mock do frontend (`selectedEventIds`), que nao corresponde ao contrato validado pelo backend desde a Sprint 23. (6) `gallery`/`feature-grid`/`card-list`/`hero` nao precisaram de nenhuma relaxacao de validacao — um item-placeholder estrutural sem referencia real ja passa na validacao existente. (7) `label` do texto da etapa (`card-list "Vagas"`) mapeado para `PageSection.variant`, campo ja existente com esse proposito. (8) Self-invocation de metodo `@Transactional` evitada em `NotificationService` (`onTenantStatusChanged`/`notifyTenantStatusChange`, java:S6809) extraindo `doNotifyTenantStatusChange` privado nao-transacional, mesmo padrao ja usado em `ContentService.doTransition` (Sprint 11).

**SonarQube for IDE:** literais repetidos extraidos para constantes (`java:S1192`) em `BlockDefaults` (`KEY_TITLE`, `KEY_BODY`, `KEY_ITEMS`, `KEY_SOURCE`) e `PageTemplateCatalog` (`SLUG_HOME`, `TITLE_HOME`); self-invocation `@Transactional` corrigida em `NotificationService` (java:S6809, decisao 8 acima). Rodada dedicada adicional corrigiu 4 apontamentos reais: `java:S125` em `BlockDefaults` (comentario reescrito em prosa continua — a forma anterior disparava o heuristico de "codigo comentado"); `java:S5838` em `BlockDefaultsTest` (`isEqualTo("")` trocado por `isEmpty()`); `java:S5778` em `ProductServiceTest` (lambda de `assertThatThrownBy` com tres invocacoes — `caller`/`command` extraidos para variavel local); `java:S5853` em `ProductTemplateScaffoldIntegrationTest` (asseroes separadas sobre o mesmo sujeito encadeadas em uma unica cadeia AssertJ). Nenhum `@SuppressWarnings`/`NOSONAR` usado.

**Retrofits pendentes para etapas futuras:** adicionar `WORKFLOW` ao catalogo `ModuleKey` quando o modulo for de fato implementado, e recomenda-lo para `Produto SaaS`; alinhar `frontend/src/domains/pages/blockDefaults.ts` (`event-list`) com o contrato `source` ja validado pelo backend desde a Sprint 23.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, **1337 testes**, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`, bundle com 353 classes analisadas) e Spring Modulith aprovado (`ModulithArchitectureTest`, 1 teste, 0 falhas, 0 erros — nenhuma dependencia ciclica introduzida pelos dois novos eventos). Bruno: pasta nova `26-product-templates` com **9 requests, 9 aprovados, 100% dos testes aprovados**; a execucao da collection cumulativa inteira (245 requests) mostrou falhas em cascata pre-existentes nas pastas 23/24 (409 de slug duplicado por dados residuais do Postgres local de longa duracao), confirmadas como identicas antes de qualquer alteracao desta etapa e portanto fora de escopo — reset do volume de dados local nao foi executado por ser acao destrutiva. Evidencias completas: `results/sprint-26.md`.

## Etapa 27 — Dominio `feedback` (concluida em 2026-07-03)

**Classes criadas/alteradas:** novo modulo `br.com.byop.aegis.feedback` com `Feedback`, `FeedbackCategory`, `FeedbackPriority`, `FeedbackStatus`, `FeedbackRepository`, contracts `CreateFeedbackRequest`/`UpdateFeedbackStatusRequest`, DTO `FeedbackSummary`, mapper MapStruct `FeedbackMapper`, `FeedbackService`, `FeedbackController` e excecoes/handler especificos (`FeedbackNotFoundException`, `FeedbackForbiddenException`, `FeedbackContextRequiredException`, `FeedbackAttachmentNotFoundException`, `FeedbackExceptionHandler`). Migration `V14__feedback.sql` cria `feedback_public_id_seq` e tabela `feedback` com constraints de valores canonicos, id publico `AGS-####`, FKs para `tenants`/`products`/`assets` e indices por tenant/status/criacao. Criada porta publica `notification.api.FeedbackNotificationService` + `CriticalFeedbackNotificationRequest` para feedback critico. Alterados `tenant.api.TenantUserAccessService` (novas APIs publicas `listActiveMemberships` e `listActiveSuperAdminSubjects`), `TenantMembershipRepository`, `NotificationService`, `OpenApiConfig`/`OpenApiConfigTest` e testes correspondentes.

**Endpoints confirmados:** `POST /api/v1/feedback`; `GET /api/v1/feedback`; `GET /api/v1/tenants/{tenantId}/feedback`; `PUT /api/v1/feedback/{feedbackId}/status`. Criacao aceita qualquer usuario autenticado; listagem global e update de status sao restritos a `SUPER_ADMIN`; listagem por tenant aceita `SUPER_ADMIN` ou `TENANT_ADMIN` ativo do proprio tenant.

**Decisoes de implementacao registradas pelo GPT:** o id legivel e sequencial via PostgreSQL (`feedback_public_id_seq`) e formatado como `AGS-%04d`, mantendo o UUID interno fora do contrato principal. `POST /feedback` com `productId` deriva o tenant do produto apos `ProductAccessPort.assertAccessible`; sem `productId`, deriva o tenant somente quando o usuario tem exatamente uma membership ativa, caso contrario retorna 400 `FEEDBACK_CONTEXT_REQUIRED`. `attachmentAssetId` e validado por `asset.api` e pelo tenant do produto do asset, retornando 404 `FEEDBACK_ATTACHMENT_NOT_FOUND` em cross-tenant. Feedback nao e module-gated. Categorias/prioridades aceitam variantes sem acento/case, mas respondem no canonico da sprint. Feedback `priority = "crítica"` dispara notificacao interna `WARNING`/`BELL_ONLY` para subjects com membership ativa `SUPER_ADMIN`, via `notification.api`, sem chamar `NotificationService.create(...)` administrativo nem atravessar internals entre modulos.

**Bruno e documentacao:** criada a pasta `27-feedback` com 23 requests, cobrindo criacao com/sem anexo, notificacao de feedback critico, rejeicao de asset cross-tenant, listagem global, listagem por tenant como `TENANT_ADMIN`, bloqueios de `EDITOR` e update de status por `SUPER_ADMIN`. A pasta captura o tenant seed `clientes-beta` pela API de tenants, sem depender da execucao previa da pasta 22. `docs/api-testing/README.md`, `docs/trace/00_endpoints_esperados.md`, `results/sprint-27.md` e este consolidado foram atualizados.

**Retrofits pendentes para etapas futuras:** dispatch Telegram fica para a etapa 30, quando os campos de settings de Telegram existirem; o frontend ainda precisa trocar o anexo fake do `FeedbackModal.tsx` por `<input type="file">` real e fazer upload via `POST /products/{productId}/assets` antes do `POST /feedback`.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, **1378 testes**, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado pela suite completa. Bruno da pasta 27 isolada contra a aplicacao local atual: **23 requests, 23 aprovados, 40/40 testes aprovados**. A execucao cumulativa completa continua sujeita as falhas pre-existentes de estado residual em pastas antigas ja registradas na Sprint 26; a pasta 27 passou 100% isoladamente. Evidencias completas: `results/sprint-27.md`.

## Etapa 28 — Backup e exportação de dados na exclusão de produto ou tenant (concluída em 2026-07-03)

**Objetivo:** impedir exclusão irreversível de produto ou tenant antes de gerar backup completo, armazenar o ZIP em área permanente, validar integridade no mesmo caminho usado pelo download e disponibilizar o arquivo por streaming via token temporário.

**Classes criadas:** novo subdomínio `product.export` com `ExportController`, `ProductDeleteController`, `ProductDeleteService`, `ExportAndDeleteService`, `ProductExportSerializer`, `ExportZipBuilder`, `ExportStorageService`, `ExportIntegrityService`, `ProductExportDeletionService`, `ProductExportEmailService`, `ExportCleanupJob`, `ExportToken`, `ExportTokenStatus`, `ExportTokenRepository`, contracts/DTOs e exceções/handler próprios. Portas públicas `ProductExportStoragePort`, `ProductExportStoredFile`, `TenantProductExportPort`, `TenantExportRemovalPort`; adapter `asset.api.ProductExportStorageAdapter`; service público `TenantExportRemovalService`; DTO `TenantDeleteAcceptedResponse`.

**Classes alteradas:** `Product`/`ProductStatus` ganharam ciclo `DELETING`, `DELETED`, `EXPORT_FAILED`, `DELETE_FAILED`; `TenantController`/`TenantService` passaram a retornar `202` no delete e disparar exports; `SecurityConfig` liberou o download público por token; `StorageProvider`, `LocalStorageProvider` e `S3StorageProvider` ganharam abertura/deleção de assets/exports por streaming; testes relacionados de product, tenant, security, storage e settings foram ajustados.

**Arquivos criados/alterados/removidos:** criada a migration `V15__export_tokens.sql`, o template `infra/keycloak/themes/aegis/email/html/productExport.ftl`, a pasta Bruno `28-backup-exportacao-exclusao` e `results/sprint-28.md`. Alterado `backend/pom.xml` para o driver PostgreSQL ficar no escopo padrão, pois `ProductExportSerializer` referencia `PGobject` em produção. Nenhum arquivo removido. `docs/sprints/backend/30_seed_homologacao_e_remocao_seed_java.md` aparece modificado no worktree, mas é alteração preexistente fora do escopo desta sprint.

**Endpoints confirmados:** `DELETE /api/v1/products/{productId}` retorna `202 Accepted`; `GET /api/v1/exports/{tokenId}/download` faz streaming público do ZIP por token temporário; `DELETE /api/v1/tenants/{tenantId}` retorna `202 Accepted` e agenda export/delete por produto.

**Contratos implementados:** `DeleteProductRequest` (`confirmationText`), `DeleteAcceptedResponse`, `TenantDeleteAcceptedResponse`, erros `INVALID_PRODUCT_DELETE_CONFIRMATION`, `EXPORT_ALREADY_IN_PROGRESS`, `PRODUCT_DELETE_FORBIDDEN`, `EXPORT_DOWNLOAD_NOT_FOUND` e `EXPORT_LINK_EXPIRED`.

**Decisões técnicas registradas:** o delete só ocorre depois de serializar dados, montar ZIP, armazenar em área permanente, criar token, reabrir o ZIP por streaming e validar integridade com `ZipInputStream`. O arquivo temporário não é fonte de verdade. Falhas antes do delete marcam `EXPORT_FAILED`; falhas durante delete marcam `DELETE_FAILED`. Integrações entre módulos usam portas públicas para preservar Spring Modulith.

**Estratégia de exportação ZIP e estrutura implementada:** JSON por domínio + arquivos físicos. Entradas: `manifest.json`, `product.json`, `modules.json`, `content/entries.json`, `pages/pages.json`, `forms/forms.json`, `forms/submissions.json`, `assets/metadata.json`, `assets/files/{category}/...`, `knowledge-graph/nodes.json`, `knowledge-graph/edges.json`, `users/assignments.json`, `audit/events.json`. Assets são lidos por streaming do storage local/S3 e gravados no ZIP sem carregar arquivos inteiros em memória.

**Storage dos exports:** local em `exports/{tokenId}/{filename}` e S3 em `aegis/pms/exports/{tokenId}/{filename}`. A validação de integridade reabre exatamente o arquivo armazenado que será servido no download.

**Template de e-mail:** `productExport.ftl` ficou em `infra/keycloak/themes/aegis/email/html`, mantendo os templates do Aegis junto ao tema/e-mails já existentes. O ZIP não é anexado; o e-mail leva link temporário.

**Executor assíncrono:** bean `exportTaskExecutor`, prefixo `aegis-export-`, core pool size `2`, max pool size `4`, queue capacity `25`. Justificativa: limitar concorrência de operação pesada de I/O/banco, permitindo paralelismo moderado para tenants com múltiplos produtos.

**Expiração dos links:** `ExportToken` expira em 7 dias (`createdAt.plus(7, DAYS)`); `manifest.downloadExpiresAt` também usa 7 dias. Cleanup diário às 03:00 (`0 0 3 * * *`) remove ZIP expirado e marca token `EXPIRED`.

**Comportamento em falhas:** falha antes do e-mail pronto preserva dados e marca `EXPORT_FAILED`; falha no e-mail pronto também impede delete e marca `EXPORT_FAILED`; falha no delete depois do export válido marca `DELETE_FAILED` e não faz retry silencioso.

**Funcionamento dos endpoints:** `DELETE /products/{productId}` valida confirmação textual, permissão e idempotência, marca `DELETING`, retorna `202` e dispara o job. `DELETE /tenants/{tenantId}` lista produtos ativos e dispara export/delete por produto antes da remoção final do tenant. `GET /exports/{tokenId}/download` valida token/status/expiração, registra primeiro `downloadedAt` e transmite o ZIP via `StreamingResponseBody`.

**Testes executados e Maven:** teste focado dos arquivos Sonar passou com 21 testes, 0 falhas e 0 erros. `mvn clean verify` passou com **BUILD SUCCESS**, **1454 testes**, 0 falhas, 0 erros, 0 ignorados.

**JaCoCo e Spring Modulith:** JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado pela suíte completa, incluindo `ModulithArchitectureTest`.

**Bruno:** pasta `28-backup-exportacao-exclusao` criada e ajustada para execução isolada com login próprio. Validação executada com `npx @usebruno/cli run 28-backup-exportacao-exclusao --env local`: **6 requests, 6 aprovados, 11/11 testes**.

**Validação MailHog:** executada com backend local, Keycloak em `8282` e MailHog em `8025`. E-mail recebido de `noreply@aegis.app` para `alexandre.henrique.dev@gmail.com` com assunto `Exportacao de dados pronta no Aegis PMS`. Link de download retornou `200`, ZIP passou em `unzip -t` sem erros e o produto validado passou a retornar `404 PRODUCT_NOT_FOUND`, confirmando delete após backup íntegro e disponível para streaming.

**SonarQube for IDE:** corrigidos apontamentos reportados sem `@SuppressWarnings`/`NOSONAR`: `throws` desnecessários em testes, import morto, temp dir público em `ExportZipBuilder`, reflexão por nome de classe trocada por `instanceof PGobject`, campo/import mortos, SQL restrito a enums de queries fixas, redução de métodos com parâmetros excessivos e lambdas de `assertThatThrownBy` com uma chamada potencialmente lançadora. Reanálise final do painel da IDE não tem runner CLI neste ambiente.

**Mitigações em AGENTS.md:** nenhuma mitigação nova adicionada nesta execução; os ajustes seguiram os padrões já registrados no `AGENTS.md`, especialmente `@Autowired` em beans com múltiplos construtores, `PathVariable` explícito e lambdas de `assertThatThrownBy`.

**Retrofits pendentes:** formalizar retry/backoff para SMTP/S3 se necessário e avaliar retenção configurável de exports por tenant/plano.

**Critérios de aceite atendidos:** `DELETE /products` 202, `DELETE /tenants` assíncrono com backup por produto, ZIP com JSONs por domínio e assets físicos, download por token temporário, delete somente após ZIP armazenado/reaberto/validado, falhas preservando dados, Maven/Jacoco/Modulith aprovados, Bruno 28 aprovado e MailHog validado. Evidências completas: `results/sprint-28.md`.

## Etapa 29 — Tokens de ativação de convite e reset de senha via Aegis

**Objetivo:** substituir os links/e-mails de ação do Keycloak por tokens próprios do Aegis, mantendo o usuário final nas telas do frontend Aegis para ativação de convite e redefinição de senha.

**Classes criadas/alteradas:** criado o domínio `identity.auth.domain.AuthActionToken` com `AuthActionType`, `AuthActionStatus`, repository, migration `V16__auth_action_tokens.sql`, `AuthActionTokenService`, `AuthActionEmailService`, `AuthActivationService`, `PasswordPolicy` e `AuthActionTokenCleanupJob`. Criados DTOs/erros públicos para token de ação e service público `identity.api.IdentityActionTokenService` com `IdentityActionInviteCommand`. Criado evento público `IdentityAuthActionAuditEvent` e listener no módulo `audit` para preservar Spring Modulith sem dependência direta `identity -> audit`. Alterados `AuthController`, `AuthExceptionHandler`, `AuthService`, `KeycloakAdminClient`, `IdentityUserLifecycleService`, `TenantUserService`, `ProductAssignmentService`, `ProductAssignmentInvitePort` e `KeycloakProductAssignmentInvitePort`.

**Endpoints confirmados:** `POST /api/v1/auth/invite/validate`, `POST /api/v1/auth/activate`, `POST /api/v1/auth/reset-password/request` e `POST /api/v1/auth/reset-password/confirm`. Todos são públicos sob `/api/v1/auth/**`, sem expor o token por query string em validação.

**Decisões de implementação registradas:** convites expiram em 48h e reset em 1h; token fica `PENDING`, `USED` ou `EXPIRED`; reset invalida links pendentes anteriores do mesmo usuário e aplica rate limit manual de 3 solicitações por e-mail em 15 minutos; `executeActionsEmail` permanece no client apenas como API opcional/legada para debug, mas não é chamado pelos fluxos produtivos de convite/reset. Ao ativar convite, o backend define senha no Keycloak, habilita usuário e limpa `requiredActions`. Falha de política de senha do Keycloak é normalizada para `WEAK_PASSWORD`.

**Templates e configuração:** criados `infra/keycloak/themes/aegis/email/html/inviteActivation.ftl` e `passwordReset.ftl`, com links para `${AEGIS_APP_BASE_URL}/invite?token=...` e `${AEGIS_APP_BASE_URL}/reset-password?token=...`. `docker-compose.yml`, `.env.example` e o profile `local` receberam fallback/configuração `AEGIS_APP_BASE_URL=http://localhost:5173`.

**Bruno:** criada e executada a pasta `29-auth-action-tokens` com fluxo isolado: login Loki, criação de tenant descartável, convite de usuário, captura de `inviteActivationToken` no MailHog via `/api/v2/messages`, validação do convite, rejeição de senha fraca, ativação, solicitação de reset, captura de `passwordResetToken`, confirmação de reset e contrato de erro `TOKEN_NOT_FOUND`. Resultado final: **11/11 requests aprovados e 22/22 testes aprovados** contra backend local, Keycloak e MailHog.

**SonarQube for IDE:** rodada reportada em 2026-07-03 corrigiu 7 apontamentos sem supressão: `java:S2068` em constantes de e-mail/política de senha (`PASSWORD` removido dos nomes internos), `java:S7467` com unnamed variable no `catch`, `java:S1874` trocando `UNPROCESSABLE_ENTITY`/`isUnprocessableEntity` por `UNPROCESSABLE_CONTENT`/`isUnprocessableContent`, `java:S1128` removendo import morto e `java:S1161` adicionando `@Override` no adapter de convite por produto.

**Cobertura e testes:** após a rodada Sonar, o teste focal `mvn -q -Dtest='AuthControllerTest,AuthExceptionHandlerTest,PasswordPolicyTest,AuthActivationServiceTest,KeycloakProductAssignmentInvitePortTest' test` passou. `mvn clean verify` fechou com `BUILD SUCCESS`, **1502 testes**, 0 falhas, 0 erros, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado pela suíte completa. MailHog foi validado com links `http://localhost:5173/invite?token=<uuid>` e `http://localhost:5173/reset-password?token=<uuid>`.

**Retrofits pendentes:** implementar/validar telas frontend reais `/invite` e `/reset-password`; validar login real no frontend após ativação/reset quando as telas existirem; avaliar remoção futura dos wrappers legados `executeActionsEmail` quando não forem mais necessários para debug. Evidências completas: `results/sprint-29.md`.

## Etapa 30 — Migration de dados reais de homologação e remoção do seed Java

**Objetivo:** substituir o seed Java local por uma migration Flyway idempotente com dados reais de homologação do tenant `CLIENTES BETA`, remover APIs auxiliares de seed e fechar retrofits pendentes das etapas anteriores. A regra de Telegram ficou explícita: feedback/bug report do próprio Aegis é uma jornada interna (`usuário -> tela do Aegis -> banco -> Telegram global do Aegis`); report/submission de produto administrado pelo Aegis é outra jornada (`plataforma externa -> formulário -> banco -> Telegram configurado no formulário/produto`).

**Arquivos criados:** migration `backend/src/main/resources/db/migration/V17__seed_homologacao.sql`; contexto HTTP de auditoria (`AuditContext`, `AuditContextHolder`, `AuditRequestContextInterceptor`, `AuditWebMvcConfig`); `TelegramFeedbackNotifier`; `FormSubmissionTelegramNotifier`; API pública `settings.api.TelegramAlertSettingsService`/`TelegramAlertSettings`; testes de auditoria, Telegram, settings e grafo; pasta Bruno `30-seed-homologacao`; evidência final `docs/sprints/backend/results/sprint-30.md`.

**Arquivos alterados:** `.env.example`, `application.yml`, `application-local.yml`, `OpenApiConfig`, `AuditService`, `AuditRiskCatalog`, `FeedbackService`, `FormReference`/`FormReferenceService`, `SubmissionService`, `ProductController`, `ProductModuleService`, `ProductSecuritySettings`/contracts/DTOs/mapper/service, enums de Knowledge Graph, realm export do Keycloak, Bruno 24, documentação de API testing, qualidade, feedback backend/frontend, sprint 30, consolidado da sprint e `AGENTS.md`.

**Arquivos removidos:** package `br.com.byop.aegis.seed` (`DemoSeedRunner`, `DemoSeedService`) e testes; APIs públicas exclusivas do seed em `tenant.api`, `product.api` e `knowledgegraph.api` (`TenantSeed*`, `ProductSeed*`, `KnowledgeGraphSeedService`, `GraphSeed*`) e seus testes. `NotificationOnboardingService` foi preservado porque continua sendo usado no fluxo real de convite/onboarding por `TenantUserService`.

**Migration V17 e dados reais:** `V17__seed_homologacao.sql` adiciona `telegram_alert_chat_id` e `telegram_alert_bot_token` em `product_security_settings`; cria o tenant `clientes-beta`/`CLIENTES BETA`; cria seis produtos reais (`maestro-beton`, `conecta-talentos`, `alexandre-dev`, `cmss`, `wikidev`, `loki`); cria módulos, páginas, seções e Knowledge Graph inicial. A migration usa `ON CONFLICT DO NOTHING` e `WHERE EXISTS` para preservar bancos já populados; em banco local antigo com produtos da mesma `key`, dados residuais são mantidos e a pasta Bruno 30 valida esse cenário sem reset destrutivo.

**Páginas e grafo:** Maestro Beton recebeu páginas `home`, `quem-somos`, `historia`, `agenda`, `galeria`, `apoie` e `contato`; CMSS recebeu `home`, `quem-somos`, `historia`, `agenda`, `apoie` e `contato`; Conecta Talentos recebeu `home`; Alexandre Dev recebeu `home`. WikiDev recebeu nodes `Spring Boot`, `JPA`, `Docker`; Loki recebeu `Vigília`, `Clair de Lune - Debussy`, `Manifesto do Silêncio`, `Playlist: Introspecção`, `Fragmentos`; Maestro Beton recebeu nodes de produto, página, form, categoria e autora. Foram adicionados valores Java `TENANT`, `PLAYLIST`, `MUSIC_REF`, `MANIFEST` em `GraphNodeType` e `INTEGRATES_WITH`, `PACKAGED_BY`, `WRITTEN_BY` em `GraphEdgeType`.

**Retrofits concluídos:** enable/disable de módulos agora registra auditoria `MODULE_ENABLED`/`MODULE_DISABLED`; submissão pública registra `FORM_SUBMISSION_RECEIVED`; auditoria passa a enriquecer eventos com `traceId`, `ip` e `userAgent`; feedback crítico continua notificando o domínio `notification` e, quando configurado, o Telegram global do Aegis; submissions externas passam a enviar Telegram por delivery channel do formulário/produto, best-effort. As integrações entre módulos seguem por APIs públicas e Spring Modulith permaneceu aprovado.

**Swagger/Keycloak:** `OpenApiConfig` passou a expor `oauth2-pkce` com Authorization Code + PKCE preservando `bearer-jwt`; `application-local.yml` configura o OAuth do Swagger UI; `infra/keycloak/realm/aegis-realm.json` inclui `http://localhost:8080/swagger-ui/oauth2-redirect.html` como redirect URI.

**SonarQube for IDE:** corrigidos os apontamentos reportados em 2026-07-03 sem supressão: `java:S6068` em `ProductControllerTest` removendo `eq(...)` inútil, e `java:S6809` em `ProductModuleService` evitando self-invocation transacional por meio de métodos privados comuns. Nenhum `@SuppressWarnings`/`NOSONAR` novo foi adicionado; as duas ocorrências encontradas por grep (`SecurityConfig` e `AuditEventQueryService`) são preexistentes e fora do escopo.

**Bruno e documentação:** pasta `30-seed-homologacao` criada com login SUPER_ADMIN, captura do tenant `clientes-beta`, captura dos produtos reais, validações de páginas/grafo, atualização de Telegram do produto e validação OpenAPI OAuth2 PKCE. Resultado isolado: **7 requests, 7 aprovados, 16/16 testes aprovados**. A collection cumulativa também foi executada e falhou por divergências já conhecidas em pastas antigas/estado residual (**292 requests, 267 aprovados, 25 falhos, 530/569 testes**); a pasta 30 passou dentro da cumulativa e isoladamente. Documentações de feedback, frontend, qualidade, API testing e `AGENTS.md` foram alinhadas para registrar a separação obrigatória entre Telegram interno do Aegis e Telegram de produto externo.

**Cobertura e testes:** `mvn clean verify` fechou com `BUILD SUCCESS`, **1504 testes**, 0 falhas, 0 erros, 0 ignorados, JaCoCo aprovado (`All coverage checks have been met`) e Spring Modulith aprovado pela suíte completa. Testes focados de feedback, submissão, Telegram, settings, OpenAPI, auditoria, módulos e grafo também passaram antes do verify completo.

**Pendências conscientes:** validar manualmente recebimento real no Telegram global do Aegis com `AEGIS_TELEGRAM_ALERT_ENABLED=true`, token e chat reais; validar manualmente submission externa com canal Telegram real; validar OAuth2 PKCE na UI do Swagger com Keycloak local no navegador. Evidências completas: `results/sprint-30.md`.

## Correção operacional — CORS de produção e deploy automatizado (concluída em 2026-07-24)

**Classes criadas/alteradas:** criados `shared.config.AegisAppProperties`,
`shared.config.AegisApplicationConfig` e a `NamedInterface` `shared.config`;
`SecurityConfig` passou a consumir propriedades tipadas e imutáveis em vez de
`@Value` sobre lista YAML. `SecurityConfigTest` cobre origem permitida, origem
negada e preflight; `AegisAppPropertiesTest` cobre YAML de `prod`, default local,
variável de ambiente separada por vírgula, imutabilidade e rejeição de wildcard.

**Endpoints confirmados:** nenhum endpoint novo. Foram validados
`GET /actuator/health`, `OPTIONS /api/v1/auth/login` e
`POST /api/v1/auth/login` com credenciais propositalmente inválidas.

**Decisões de implementação registradas pelo GPT:** lista de origens usa
`@ConfigurationProperties`; apenas origens HTTP(S) explícitas sem wildcard são
válidas; `allowCredentials(true)` e múltiplas origens foram preservados;
`AEGIS_DEPLOY_BRANCH` é a variável canônica (`develop` hoje, `release` no
futuro), e `IMAGE_TAG` deriva dela. O Dockerfile fixa o modo API e as URLs
públicas do build Vite. O deploy oficial usa `scripts/deploy-aegis.sh`, compara
IDs de imagem, valida health/CORS/login inválido e executa rollback automático.
O workflow observa `develop`/`release`, mas só implanta a branch configurada.

**Retrofits pendentes:** revogar no Keycloak as sessões atuais de
`aegis@buildyourownpath.io` após o teste manual de login real; preparar
`.env.release` somente antes da promoção real. As três vulnerabilidades
transitivas inicialmente reportadas por `npm audit` foram corrigidas no
lockfile em 2026-07-24 e o workflow passou a bloquear novas vulnerabilidades
altas ou críticas.

**Cobertura de testes:** `mvn clean verify` com `BUILD SUCCESS`, 1681 testes,
zero falhas/erros e JaCoCo 100%; Modulith aprovado. Frontend typecheck, lint e
build API aprovados; `npm audit` retorna zero vulnerabilidades. Docker build sem cache aprovado para
`aegis-pms:develop`; Compose, scripts, workflow YAML e diff validados. Evidências
detalhadas: `results/cors-prod-deploy-automation.md`.

**Correção pós-instalação do runner:** o job `backend-verify` passou a provisionar
PostgreSQL 16 efêmero em `localhost:5434`, com credenciais descartáveis iguais
aos defaults de teste. Isso torna o `mvn verify` independente de serviços
preexistentes no host e mantém os testes isolados do banco de produção. O log
real do runner revelou depois que `actions/setup-java` não instala o comando
`mvn`; o backend passou a versionar o Maven Wrapper 3.9.16, com checksum da
distribuição, e o workflow executa `./mvnw -B clean verify`. A primeira versão
usava o modo `only-script`, que no host sem `unzip` substituiu o ZIP por
`tar.gz` sem substituir o checksum e falhou antes do Maven iniciar. O wrapper
foi migrado para o modo `bin`, com o JAR oficial versionado e validado por
checksum, eliminando dependências globais de Maven e `unzip`.

**Correção do job Bruno no runner:** o job passou a configurar Node 22
explicitamente, validar a disponibilidade de `crypto.subtle` e fixar o Bruno
CLI 4.0.0, porque jobs distintos não herdam o Node preparado no job frontend.
Backend e MailHog da stack efêmera deixaram de disputar as portas 8080 e 8025
do host: são publicados em portas aleatórias limitadas a `127.0.0.1`. O
Keycloak de CI usa a porta dedicada 18282. Os endpoints são descobertos e
injetados na collection como `baseUrl`, `mailhogBaseUrl` e `keycloakIssuer`; o
SMTP permanece exclusivamente interno à rede da stack.

As capturas de convite e redefinição de senha também passaram a aguardar, por
polling limitado, a entrega assíncrona da mensagem ao destinatário esperado no
MailHog. Isso elimina a condição de corrida em que a API retornava `items: []`
imediatamente após a criação do convite. Validação local equivalente ao runner:
337/337 requisições e 650/650 testes Bruno aprovados com Node 22 e CLI 4.0.0.

**Correção do hardening no deploy:** a URL-base pública do Keycloak passou a
ser lida do arquivo operacional externo, com valor
`https://auth.buildyourownpath.io` e sem o caminho do realm. O script recebeu
suporte a `--env-file` com lista permitida de chaves e sem executar o arquivo
como shell. O realm foi alinhado para `aegis-pms`, e URL, credenciais
administrativas e SMTP deixaram de ser duplicados no GitHub. O `kcadm` é
fornecido por um container efêmero da imagem oficial do Keycloak, eliminando a
dependência de instalação do CLI no runner.

O teste operacional confirmou que o proxy público aceitava autenticação, mas
bloqueava a operação administrativa com `403`; o mesmo hardening passou ao
resolver `auth.buildyourownpath.io` para `10.200.0.1` pela WireGuard. A rota foi
centralizada em `KEYCLOAK_RESOLVE_IP`, aplicada somente ao container via
`--add-host`, sem trocar a URL HTTPS nem comprometer SNI/TLS.
