/** Shared fade-in transition preset for `motion.div`/`motion.header` mounts across the app. */
export const fade = { initial: { opacity: 0, y: 10 }, animate: { opacity: 1, y: 0 }, transition: { duration: 0.2 } };
