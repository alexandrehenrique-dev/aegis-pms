$kind: http-request
url: "{{baseUrl}}/api/v1/products/{{productId}}/users/{{userId}}"
method: DELETE
headers:
  Authorization: Bearer {{token}}
scripts:
  - type: afterResponse
    code: pm.test('status 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 8000
