$kind: http-request
url: "{{backendBaseUrl}}/actuator/info"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is 200', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
